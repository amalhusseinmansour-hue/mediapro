# ⚡ دليل الإجراء السريع لإطلاق التطبيق

**الوقت المتوقع:** 15 دقيقة  
**الصعوبة:** سهلة جداً

---

## 🎯 الخطوات السريعة

### Step 1: تحديث مفتاح Paymob (5 دقائق) 🔑

```bash
# 1. افتح المتصفح واذهب إلى:
https://accept.paymob.com/portal2/en/settings

# 2. ابحث عن "API Key" أو "Integration Key"

# 3. انسخه (مثال):
ZXlKaGJHY2lPaUpJVXpVeE1p...

# 4. افتح الملف:
lib/core/config/api_config.dart

# 5. ابحث عن هذا السطر:
static const String paymobApiKey = String.fromEnvironment(
  'PAYMOB_API_KEY',
  defaultValue: 'ZXlKaGJHY2lPaUpJVXpVeE1p...', // هنا!
);

# 6. استبدل المفتاح القديم بالجديد
# 7. احفظ الملف (Ctrl+S)
```

### Step 2: إعادة بناء التطبيق (10 دقائق) 🔨

```bash
# افتح Terminal وقم بتشغيل هذه الأوامر:

# من مجلد المشروع:
cd c:\Users\HP\social_media_manager

# 1. تنظيف البناء السابق
flutter clean

# 2. جلب الحزم
flutter pub get

# 3. إعادة بناء التطبيق
flutter run

# أو إذا كنت على الويب:
flutter run -d chrome
```

### Step 3: اختبار الاشتراك (5 دقائق) ✅

```
من التطبيق:
1. اذهب إلى "الاشتراكات" أو "المزايا"
2. انقر على "ترقية الخطة"
3. اختر خطة (مثلاً Professional)
4. انقر "اشتراك الآن"
5. ستظهر نموذج الدفع عبر Paymob
6. ✅ إذا لم تظهر رسالة خطأ، فالمفتاح صحيح!
```

---

## 🔍 التحقق السريع من كل شيء

### ✅ التحقق 1: الاتصال بقاعدة البيانات
```bash
# من Terminal:
curl https://mediaprosocial.io/api/health

# النتيجة المتوقعة:
# {"status":"ok","timestamp":"...","database":"connected"}
```

### ✅ التحقق 2: البيانات المحفوظة
```bash
# من phpMyAdmin:
# 1. اذهب إلى https://mediaprosocial.io/phpmyadmin
# 2. أدخل البيانات
# 3. اختر جدول "users"
# ✅ يجب أن ترى المستخدمين الجدد
```

### ✅ التحقق 3: سجلات API
```bash
# من phpMyAdmin:
# 1. اختر جدول "api_logs"
# ✅ يجب أن ترى طلبات API الأخيرة
```

---

## 🎬 تشغيل الاختبار الكامل

```bash
# إذا أردت اختبار شامل:

# 1. اقرأ: COMPREHENSIVE_TESTING_GUIDE.md
# 2. اتبع جميع الخطوات
# 3. سيستغرق حوالي 30 دقيقة
```

---

## ⚠️ حل المشاكل السريع

### ❌ خطأ: "Connection refused"
```
الحل:
- تحقق من اتصال الإنترنت
- جرب مع VPN
- تأكد من أن الخادم يعمل
```

### ❌ خطأ: "Paymob 403 error"
```
الحل:
1. تحقق من مفتاح API
2. تأكد أنه ليس منتهي الصلاحية
3. أعد بناء التطبيق: flutter clean && flutter run
```

### ❌ خطأ: "Database connection failed"
```
الحل:
1. تحقق من بيانات الاتصال في .env
2. تأكد من أن الخادم يستجيب: ping 82.25.83.217
3. تحقق من الجدران النارية
```

### ❌ خطأ: "Failed to rebuild"
```
الحل:
1. حذف pubspec.lock: rm pubspec.lock
2. جلب الحزم مرة أخرى: flutter pub get
3. أعد البناء: flutter run
```

---

## 📊 نقاط تفتيش النجاح

| النقطة | الحالة | الملاحظة |
|--------|--------|---------|
| Paymob API محدث | ✅ | لا تنسى! |
| التطبيق يعمل | ✅ | بدون أخطاء |
| قاعدة البيانات تستجيب | ✅ | health check |
| المستخدم يسجل دخول | ✅ | OTP يعمل |
| الحساب يربط | ✅ | connected_accounts |
| المنشور ينشر | ✅ | auto_scheduled_posts |
| الاشتراك يعمل | ✅ | Paymob يعمل |
| البيانات محفوظة | ✅ | في قاعدة البيانات |

---

## 🚀 الإطلاق النهائي

```bash
# عندما تكون جاهز للإطلاق الحقيقي:

# 1. بناء الإصدار (Release)
flutter build apk --release    # للـ Android
flutter build ios --release    # للـ iOS
flutter build web --release    # للـ Web

# 2. التحميل إلى متجر التطبيقات
# Google Play Store (Android)
# Apple App Store (iOS)
# Progressive Web App (Web)

# 3. أخبر المستخدمين عن الإطلاق
```

---

## 💡 نصائح مهمة

1. **احفظ مفاتيح API آمنة**
   - لا تضعها في Git
   - استخدم متغيرات البيئة

2. **راقب الأداء**
   - تحقق من سجلات API
   - راقب استخدام الموارد

3. **عدل الأخطاء سريعة**
   - لا تنتظر، عدّل فوراً
   - اختبر التصحيح

4. **احتفظ بنسخة احتياطية**
   - من قاعدة البيانات يومياً
   - من الكود أسبوعياً

---

## 📋 قائمة التحقق النهائية

قبل الإطلاق، تأكد من:

- [ ] مفتاح Paymob محدث ✅
- [ ] التطبيق يعمل بدون أخطاء ✅
- [ ] قاعدة البيانات تستجيب ✅
- [ ] جميع الجداول موجودة ✅
- [ ] الاختبارات تمرت ✅
- [ ] البيانات تُحفظ بشكل صحيح ✅
- [ ] لا توجد تحذيرات أمان ✅
- [ ] النسخة الاحتياطية موجودة ✅

---

## 🎉 تمت!

بعد اتباع هذه الخطوات:

```
✅ التطبيق جاهز للاستخدام
✅ جميع البيانات محفوظة بأمان
✅ الدفع يعمل بشكل صحيح
✅ المستخدمون يستطيعون التسجيل والدخول
✅ الحسابات تتصل بنجاح
✅ المحتوى ينشر على المنصات
```

**يمكنك الآن إطلاق التطبيق بثقة! 🚀**

---

**تم إعداد الدليل:** 2025  
**النسخة:** 1.0
