# 💳 دليل صفحة إعدادات الدفع

**تاريخ الإنشاء:** نوفمبر 2025
**الحالة:** ✅ **جاهزة بالكامل**

---

## ✅ ما تم إضافته:

تم إنشاء صفحة شاملة **Payment Settings** تحتوي على:

1. ✅ **الإحصائيات** - عرض إحصائيات الدفع
2. ✅ **إعدادات البوابات** - ربط بوابات دفع متعددة
3. ✅ **اختبار الدفع** - اختبار البوابات قبل التفعيل
4. ✅ **جدول المعاملات** - عرض جميع المعاملات

---

## 📂 الملفات المضافة:

### 1. Models (النماذج):

#### `lib/models/payment_transaction_model.dart`
```dart
/// نموذج معاملة الدفع
class PaymentTransactionModel {
  - id
  - userId
  - orderId
  - paymentKey
  - amount
  - currency
  - gateway
  - status
  - errorMessage
  - billingData
  - metadata
  - createdAt
  - completedAt
  - updatedAt
}

/// حالات الدفع
enum PaymentStatus {
  pending,      // قيد الانتظار
  processing,   // جاري المعالجة
  completed,    // مكتملة
  failed,       // فاشلة
  cancelled,    // ملغاة
  refunded,     // مستردة
}
```

#### `lib/models/payment_gateway_config_model.dart`
```dart
/// نموذج إعدادات بوابة الدفع
class PaymentGatewayConfigModel {
  - id
  - name (paymob, stripe, paytabs, checkout)
  - displayName
  - isEnabled
  - isTestMode
  - credentials (Map<String, String>)
  - settings
  - supportedCurrencies
  - feePercentage
  - fixedFee
  - priority
  - createdAt
  - updatedAt
}

/// البوابات الافتراضية
- Paymob (رسوم 2.9% + 1 د.إ)
- Stripe (رسوم 2.9% + 1 د.إ)
- PayTabs (رسوم 2.85% + 0.5 د.إ)
- Checkout.com (رسوم 2.5%)
```

### 2. Services (الخدمات):

#### إضافات في `lib/services/firestore_service.dart`:

##### دوال المعاملات:
```dart
// إنشاء معاملة جديدة
Future<String?> createPaymentTransaction(PaymentTransactionModel)

// تحديث معاملة
Future<bool> updatePaymentTransaction(String transactionId, Map<String, dynamic> data)

// الحصول على معاملة
Future<PaymentTransactionModel?> getPaymentTransaction(String transactionId)

// الحصول على معاملات المستخدم
Future<List<PaymentTransactionModel>> getUserPaymentTransactions(String userId)

// الاستماع للمعاملات (Real-time)
Stream<List<PaymentTransactionModel>> listenToUserPaymentTransactions(String userId)

// الحصول على الإحصائيات
Future<Map<String, dynamic>> getPaymentStatistics(String userId)
```

##### دوال البوابات:
```dart
// حفظ إعدادات بوابة
Future<bool> savePaymentGatewayConfig(PaymentGatewayConfigModel config)

// الحصول على بوابة
Future<PaymentGatewayConfigModel?> getPaymentGatewayConfig(String gatewayId)

// الحصول على جميع البوابات
Future<List<PaymentGatewayConfigModel>> getAllPaymentGatewayConfigs()

// الاستماع للبوابات (Real-time)
Stream<List<PaymentGatewayConfigModel>> listenToPaymentGatewayConfigs()

// الحصول على البوابات المفعلة فقط
Future<List<PaymentGatewayConfigModel>> getEnabledPaymentGateways()

// حذف بوابة
Future<bool> deletePaymentGatewayConfig(String gatewayId)
```

### 3. Controller:

#### `lib/controllers/payment_settings_controller.dart`:

##### Observable States:
```dart
- isLoading
- isTesting
- selectedTab (statistics, gateways, test, transactions)
- totalAmount
- successfulAmount
- totalCount
- successfulCount
- failedCount
- pendingCount
- successRate
- gateways (List<PaymentGatewayConfigModel>)
- transactions (List<PaymentTransactionModel>)
```

##### الدوال:
```dart
loadData()                                  // تحميل جميع البيانات
loadStatistics()                            // تحميل الإحصائيات
loadGateways()                              // تحميل البوابات
loadTransactions()                          // تحميل المعاملات
startListening()                            // بدء الاستماع للتحديثات
stopListening()                             // إيقاف الاستماع
saveGatewayConfig(config)                   // حفظ إعدادات بوابة
testGateway(gateway)                        // اختبار بوابة
deleteGateway(gatewayId)                    // حذف بوابة
changeTab(tab)                              // تغيير التبويب
refresh()                                   // تحديث البيانات
exportTransactionsToCSV()                   // تصدير المعاملات
getTransactionsByStatus(status)             // فلترة حسب الحالة
getTransactionsByGateway(gateway)           // فلترة حسب البوابة
```

### 4. Screen:

#### `lib/screens/payment/payment_settings_screen.dart`:

واجهة شاملة بـ 4 تبويبات:

---

## 🎯 الأقسام الرئيسية:

### 1️⃣ قسم الإحصائيات (Statistics):

#### البطاقات المعروضة:
```
✅ إجمالي المبلغ - المبلغ الكلي لجميع المعاملات
✅ المبلغ الناجح - مجموع المعاملات الناجحة
✅ العدد الكلي - عدد جميع المعاملات
✅ الناجحة - عدد المعاملات الناجحة
✅ الفاشلة - عدد المعاملات الفاشلة
✅ قيد الانتظار - عدد المعاملات المعلقة
✅ نسبة النجاح - نسبة المعاملات الناجحة مع Progress Bar
```

#### الميزات:
- ✅ تحديث تلقائي عند إضافة معاملة جديدة
- ✅ Pull to refresh
- ✅ ألوان ديناميكية حسب الحالة
- ✅ أيقونات واضحة

---

### 2️⃣ قسم البوابات (Gateways):

#### البوابات المتاحة:

##### 1. Paymob
```
الاسم: Paymob
العملات: AED, EGP, USD, SAR
الرسوم: 2.9% + 1 د.إ

الحقول المطلوبة:
- API Key (سري)
- Integration ID
- iFrame ID
```

##### 2. Stripe
```
الاسم: Stripe
العملات: AED, USD, EUR, GBP, SAR
الرسوم: 2.9% + 1 د.إ

الحقول المطلوبة:
- Publishable Key
- Secret Key (سري)
- Webhook Secret (اختياري - سري)
```

##### 3. PayTabs
```
الاسم: PayTabs
العملات: AED, SAR, USD, EGP
الرسوم: 2.85% + 0.5 د.إ

الحقول المطلوبة:
- Server Key (سري)
- Profile ID
- Merchant Email
```

##### 4. Checkout.com
```
الاسم: Checkout.com
العملات: AED, USD, EUR, GBP
الرسوم: 2.5% + 0 د.إ

الحقول المطلوبة:
- Public Key
- Secret Key (سري)
```

#### الميزات:
- ✅ **Switch لتفعيل/تعطيل** البوابة
- ✅ **وضع الاختبار** (Test Mode)
- ✅ **العملات المدعومة**
- ✅ **نسبة العمولة والرسوم الثابتة**
- ✅ **الأولوية** (للترتيب)
- ✅ **حقول ديناميكية** حسب البوابة
- ✅ **إخفاء المفاتيح السرية**
- ✅ **التحقق من صحة الإعدادات**
- ✅ **أزرار الحفظ والاختبار**

#### كيفية الاستخدام:
1. اضغط على البوابة لتوسيع الإعدادات
2. أدخل المفاتيح المطلوبة من لوحة تحكم البوابة
3. فعّل/عطّل وضع الاختبار حسب الحاجة
4. اضغط "حفظ" لحفظ الإعدادات
5. اضغط "اختبار" لاختبار البوابة
6. فعّل البوابة من الـ Switch

---

### 3️⃣ قسم الاختبار (Test):

#### الميزات:
- ✅ قائمة بجميع البوابات
- ✅ حالة كل بوابة (جاهز / غير مكتمل)
- ✅ زر "اختبار" لكل بوابة
- ✅ إنشاء معاملة اختبار بمبلغ 1 درهم
- ✅ إشعارات بالنتائج
- ✅ حفظ معاملة الاختبار في قاعدة البيانات

#### كيفية الاستخدام:
1. تأكد من إدخال جميع المفاتيح المطلوبة
2. اضغط "اختبار" على البوابة المراد اختبارها
3. انتظر النتيجة (إشعار أخضر = نجح، أحمر = فشل)
4. تحقق من تفاصيل الاختبار في قسم المعاملات

---

### 4️⃣ قسم المعاملات (Transactions):

#### جدول المعاملات يعرض:
```
✅ المبلغ والعملة
✅ اسم البوابة (بالعربية)
✅ تاريخ ووقت المعاملة
✅ الحالة (بطاقة ملونة)
✅ أيقونة حسب الحالة
```

#### حالات المعاملات:
```
✅ مكتملة (أخضر) - check_circle
✅ فاشلة (أحمر) - error
⏳ قيد الانتظار (برتقالي) - pending
⏳ جاري المعالجة (برتقالي) - pending
❌ ملغاة (رمادي) - cancel
🔄 مستردة (أزرق) - replay
```

#### الميزات:
- ✅ **Real-time updates** - التحديث التلقائي
- ✅ **تصدير CSV** - تصدير جميع المعاملات
- ✅ **ترتيب** - الأحدث أولاً
- ✅ **ألوان ديناميكية** حسب الحالة
- ✅ **رسالة عند عدم وجود معاملات**

#### زر التصدير:
- يصدر جميع المعاملات بصيغة CSV
- يحتوي على: ID, Date, Amount, Currency, Gateway, Status

---

## 🔄 Real-time Updates:

### الاستماع التلقائي:
```dart
// المعاملات
_transactionsSubscription = _firestoreService
    .listenToUserPaymentTransactions(userId)
    .listen((transactions) {
      // تحديث تلقائي!
    });

// البوابات
_gatewaysSubscription = _firestoreService
    .listenToPaymentGatewayConfigs()
    .listen((gateways) {
      // تحديث تلقائي!
    });
```

### ماذا يعني هذا؟
- ✅ أي تغيير في Firestore يظهر فوراً في التطبيق
- ✅ عند إضافة معاملة جديدة، تظهر في الجدول مباشرة
- ✅ عند تحديث إعدادات بوابة، يتحدث العرض تلقائياً
- ✅ يعمل حتى بين أجهزة متعددة!

---

## 🎨 التصميم:

### الألوان:
```dart
- خلفية: darkBg (0xFF0A0A0A)
- بطاقات: darkCard (0xFF1A1A1A)
- نيون سماوي: neonCyan (0xFF00E5FF)
- نيون بنفسجي: neonPurple (0xFF7C4DFF)
- أخضر: Colors.green (ناجح)
- أحمر: Colors.red (فاشل)
- برتقالي: Colors.orange (معلق)
```

### الخطوط:
- العناوين: Bold, 24px
- العناوين الفرعية: Regular, 14px
- البيانات: Bold, 20px
- الوصف: Regular, 12px

---

## 📱 الوصول للصفحة:

### من قائمة الإعدادات:
```
1. افتح التطبيق
2. اذهب إلى "الإعدادات"
3. في قسم "إعدادات الحساب"
4. اضغط على "إعدادات الدفع"
```

### من الكود:
```dart
Get.to(() => PaymentSettingsScreen());
```

---

## 💾 قاعدة البيانات:

### Collections في Firestore:

#### 1. payment_transactions
```json
{
  "userId": "user123",
  "orderId": "order_456",
  "paymentKey": "pk_789",
  "amount": 99.0,
  "currency": "AED",
  "gateway": "paymob",
  "status": "completed",
  "errorMessage": null,
  "billingData": {...},
  "metadata": {"isTest": false},
  "createdAt": "2025-11-06T10:30:00Z",
  "completedAt": "2025-11-06T10:30:15Z",
  "updatedAt": "2025-11-06T10:30:15Z"
}
```

#### 2. payment_gateways
```json
{
  "name": "paymob",
  "displayName": "Paymob",
  "isEnabled": true,
  "isTestMode": false,
  "credentials": {
    "apiKey": "pk_live_...",
    "integrationId": "123456",
    "iframeId": "789012"
  },
  "settings": {},
  "supportedCurrencies": ["AED", "EGP", "USD", "SAR"],
  "feePercentage": 2.9,
  "fixedFee": 1.0,
  "priority": 1,
  "createdAt": "2025-11-01T00:00:00Z",
  "updatedAt": "2025-11-06T10:00:00Z"
}
```

---

## 🔐 الأمان:

### المفاتيح السرية:
- ✅ الحقول السرية تظهر بنقاط (obscureText)
- ✅ المفاتيح تُحفظ في Firestore مع Security Rules
- ✅ لا يتم عرض المفاتيح في واجهة المستخدم بعد الحفظ
- ✅ استخدام HTTPS لجميع الاتصالات

### Firebase Security Rules المقترحة:
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Payment Gateways - Admin only
    match /payment_gateways/{gatewayId} {
      allow read: if request.auth != null &&
                     get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
      allow write: if request.auth != null &&
                      get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }

    // Payment Transactions - User can only read their own
    match /payment_transactions/{transactionId} {
      allow read: if request.auth != null &&
                     resource.data.userId == request.auth.uid;
      allow create: if request.auth != null &&
                       request.resource.data.userId == request.auth.uid;
      allow update: if false; // Only backend can update
      allow delete: if false; // No deletion
    }
  }
}
```

---

## 🧪 الاختبار:

### اختبار البوابات:

#### 1. Paymob Test Mode:
```
Test Cards:
- Visa: 4987654321098769
- Mastercard: 5123456789012346
- CVV: 123
- Expiry: أي تاريخ مستقبلي
```

#### 2. Stripe Test Mode:
```
Test Cards:
- Success: 4242 4242 4242 4242
- Declined: 4000 0000 0000 0002
- CVV: أي 3 أرقام
- Expiry: أي تاريخ مستقبلي
```

#### 3. PayTabs Test Mode:
```
متاح من لوحة التحكم
```

#### 4. Checkout.com Test Mode:
```
متاح من لوحة التحكم
```

---

## 📊 إحصائيات مثال:

```
إجمالي المبلغ: 2,450.00 د.إ
المبلغ الناجح: 2,350.00 د.إ
العدد الكلي: 25 معاملة
الناجحة: 23 معاملة
الفاشلة: 1 معاملة
قيد الانتظار: 1 معاملة
نسبة النجاح: 92.0%
```

---

## 🎁 الميزات الإضافية:

### تم تنفيذها:
- ✅ دعم بوابات متعددة
- ✅ وضع الاختبار
- ✅ Real-time updates
- ✅ تصدير CSV
- ✅ إحصائيات تفصيلية
- ✅ فلترة المعاملات
- ✅ اختبار البوابات
- ✅ أمان المفاتيح

### يمكن إضافتها مستقبلاً:
- 🔄 استرجاع المدفوعات (Refunds)
- 🔄 فلترة متقدمة (بالتاريخ، المبلغ، إلخ)
- 🔄 رسوم بيانية للإحصائيات
- 🔄 إشعارات عند فشل المعاملات
- 🔄 تقارير شهرية/سنوية
- 🔄 تكامل مع أنظمة محاسبية
- 🔄 دعم Webhooks
- 🔄 سجل التغييرات (Audit Log)

---

## 🚀 كيفية الاستخدام الكامل:

### سيناريو كامل:

#### 1. إعداد البوابات:
```
1. افتح "الإعدادات" → "إعدادات الدفع"
2. انتقل إلى تبويب "البوابات"
3. اضغط على "Paymob" لتوسيع الإعدادات
4. أدخل:
   - API Key من لوحة تحكم Paymob
   - Integration ID
   - iFrame ID
5. فعّل "وضع الاختبار"
6. اضغط "حفظ"
7. اضغط "اختبار"
8. تحقق من الإشعار (أخضر = نجح)
9. إذا نجح، فعّل البوابة من الـ Switch
10. كرر مع Stripe أو أي بوابة أخرى
```

#### 2. اختبار الدفع:
```
1. انتقل إلى تبويب "الاختبار"
2. تأكد من أن البوابة "جاهز للاختبار"
3. اضغط "اختبار" على Paymob
4. انتظر الإشعار
5. انتقل إلى تبويب "المعاملات"
6. تحقق من ظهور معاملة الاختبار
7. تحقق من الحالة
```

#### 3. مراقبة المعاملات:
```
1. انتقل إلى تبويب "المعاملات"
2. شاهد جميع المعاملات في الوقت الفعلي
3. انتقل إلى تبويب "الإحصائيات"
4. شاهد نسبة النجاح والأرقام الإحصائية
5. اضغط "تصدير CSV" لتصدير البيانات
```

#### 4. التبديل للإنتاج:
```
1. عطّل "وضع الاختبار" في جميع البوابات
2. أدخل مفاتيح الإنتاج (Production Keys)
3. احفظ الإعدادات
4. اختبر مرة أخرى
5. فعّل البوابات
6. ابدأ قبول المدفوعات الحقيقية!
```

---

## 💡 نصائح:

### للمطورين:
- ✅ استخدم Test Mode دائماً أثناء التطوير
- ✅ لا تشارك المفاتيح السرية أبداً
- ✅ استخدم Environment Variables في الإنتاج
- ✅ فعّل Firebase Security Rules
- ✅ راقب المعاملات بانتظام

### للمستخدمين:
- ✅ احفظ نسخة من مفاتيح API
- ✅ اختبر البوابة قبل تفعيلها
- ✅ راقب نسبة النجاح بانتظام
- ✅ صدّر المعاملات شهرياً
- ✅ تحقق من رسوم كل بوابة

---

## 📞 الدعم:

### في حالة المشاكل:

#### 1. البوابة لا تعمل:
```
- تأكد من صحة المفاتيح
- تحقق من وضع الاختبار
- راجع logs الخطأ
- تأكد من تفعيل البوابة في لوحة تحكم المزود
```

#### 2. المعاملة فشلت:
```
- تحقق من رسالة الخطأ
- تأكد من توفر الرصيد
- تحقق من صحة بيانات البطاقة
- راجع حدود البوابة
```

#### 3. الإحصائيات غير دقيقة:
```
- انتظر قليلاً (Real-time قد يستغرق ثوانٍ)
- اسحب للتحديث
- أعد تسجيل الدخول
```

---

## ✅ الخلاصة:

### ما تم إنجازه:
1. ✅ **3 نماذج جديدة** (Transaction, Gateway Config, Gateway Field)
2. ✅ **10 دوال في Firestore Service**
3. ✅ **1 Controller شامل**
4. ✅ **1 Screen متكامل بـ 4 أقسام**
5. ✅ **تكامل مع Navigation**
6. ✅ **Real-time updates**
7. ✅ **دعم 4 بوابات دفع**
8. ✅ **اختبار شامل**
9. ✅ **تصدير CSV**
10. ✅ **إحصائيات تفصيلية**

### الصفحة جاهزة 100% للاستخدام! 🎉

**الآن يمكنك:**
- ✅ ربط أي بوابة دفع (Paymob, Stripe, PayTabs, Checkout)
- ✅ اختبار البوابات قبل التفعيل
- ✅ مراقبة جميع المعاملات في الوقت الفعلي
- ✅ عرض إحصائيات شاملة
- ✅ تصدير البيانات
- ✅ إدارة بوابات متعددة

---

**آخر تحديث:** نوفمبر 2025
**الإصدار:** 1.0.0
**الحالة:** ✅ **جاهزة للإنتاج**
