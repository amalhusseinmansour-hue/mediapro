# نسخ هذه الأوامر وتشغيلها في Terminal في cPanel

cd /home/u126213189/domains/mediaprosocial.io/public_html

# 1. تشغيل Migration
php artisan migrate --force --path=database/migrations/2025_01_09_000002_add_audience_type_to_subscription_plans.php

# 2. تشغيل Seeder
php artisan db:seed --class=SubscriptionPlanSeeder --force

# 3. مسح Cache
php artisan optimize:clear

# 4. التحقق من النتائج
php artisan tinker --execute="echo 'باقات الأفراد: ' . \App\Models\SubscriptionPlan::where('audience_type', 'individual')->count() . PHP_EOL; echo 'باقات الأعمال: ' . \App\Models\SubscriptionPlan::where('audience_type', 'business')->count() . PHP_EOL;"
