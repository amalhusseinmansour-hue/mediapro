# دليل استخدام لوحة التحكم - إدارة الطلبات

## نظرة عامة

تم إنشاء نظام متكامل لإدارة طلبات الإعلانات الممولة وطلبات المواقع الإلكترونية القادمة من التطبيق.

---

## 📋 المميزات الموجودة

### 1️⃣ **نظام طلبات الإعلانات الممولة (Sponsored Ads)**
نظام كامل لاستقبال ومعالجة طلبات الإعلانات الممولة من المستخدمين.

### 2️⃣ **نظام طلبات المواقع الإلكترونية (Website Requests)**
نظام كامل لاستقبال ومعالجة طلبات إنشاء المواقع الإلكترونية.

---

## 🗂️ الملفات الموجودة

### Models (البيانات)
```
backend/app/Models/
├── SponsoredAdRequest.php   ✅ موجود
└── WebsiteRequest.php        ✅ موجود
```

### Migrations (قواعد البيانات)
```
backend/database/migrations/
├── 2025_11_08_000001_create_website_requests_table.php        ✅ موجود
└── 2025_11_08_000002_create_sponsored_ad_requests_table.php   ✅ موجود
```

### Filament Resources (واجهة لوحة التحكم)
```
backend/app/Filament/Resources/
├── SponsoredAdRequestResource.php   ✅ موجود
└── WebsiteRequestResource.php       ✅ موجود
```

### API Controllers
```
backend/app/Http/Controllers/Api/
├── SponsoredAdRequestController.php   ✅ موجود
└── WebsiteRequestController.php       ✅ موجود
```

### API Routes
```php
// في ملف: backend/routes/api.php

// للمستخدمين - إرسال الطلبات (عام)
POST /api/website-requests
POST /api/sponsored-ad-requests

// للإدارة فقط - إدارة الطلبات (مع authentication)
GET    /api/admin/website-requests
GET    /api/admin/website-requests/statistics
GET    /api/admin/website-requests/{id}
PUT    /api/admin/website-requests/{id}
DELETE /api/admin/website-requests/{id}

GET    /api/admin/sponsored-ad-requests
GET    /api/admin/sponsored-ad-requests/statistics
GET    /api/admin/sponsored-ad-requests/{id}
PUT    /api/admin/sponsored-ad-requests/{id}
DELETE /api/admin/sponsored-ad-requests/{id}
```

---

## 🎯 طريقة الوصول للوحة التحكم

### 1. تسجيل الدخول للوحة التحكم (Filament Admin Panel)
```
URL: https://mediaprosocial.io/admin
```

### 2. القائمة الجانبية
ستجد في القائمة الجانبية قسم جديد باسم:
```
📂 إدارة الطلبات
   ├── 🌐 طلبات المواقع         (Website Requests)
   └── 📢 الإعلانات الممولة      (Sponsored Ads)
```

---

## 📊 ميزات لوحة التحكم

### صفحة طلبات المواقع الإلكترونية

#### الأعمدة المعروضة:
- **الرقم**: معرف الطلب
- **الاسم**: اسم العميل
- **البريد الإلكتروني**: بريد العميل (قابل للنسخ)
- **الشركة**: اسم الشركة (اختياري)
- **نوع الموقع**:
  - موقع شركة (Corporate)
  - متجر إلكتروني (E-commerce)
  - مدونة (Blog)
  - معرض أعمال (Portfolio)
  - صفحة هبوط (Landing Page)
  - مخصص (Custom)
- **الميزانية**: الميزانية المتوقعة بالعملة المحددة
- **الموعد النهائي**: تاريخ التسليم المطلوب
- **الحالة**:
  - ⚠️ معلق (Pending)
  - 📋 قيد المراجعة (Reviewing)
  - ✅ مقبول (Accepted)
  - ❌ مرفوض (Rejected)
  - ✔️ مكتمل (Completed)
- **تاريخ الإنشاء**

#### المرشحات (Filters):
- تصفية حسب الحالة
- تصفية حسب نوع الموقع

#### الإجراءات المتاحة:
- 👁️ **عرض**: عرض تفاصيل الطلب كاملة
- ✏️ **تعديل**: تعديل الطلب وتغيير الحالة
- 🗑️ **حذف**: حذف الطلب

#### شاشة التفاصيل تعرض:
- معلومات المستخدم (الاسم، البريد، الهاتف، الشركة)
- تفاصيل الموقع (النوع، الوصف، الميزات المطلوبة)
- الميزانية والعملة
- الموعد النهائي
- حالة الطلب
- ملاحظات الإدارة

---

### صفحة طلبات الإعلانات الممولة

#### الأعمدة المعروضة:
- **الرقم**: معرف الطلب
- **الاسم**: اسم العميل
- **البريد الإلكتروني**: بريد العميل (قابل للنسخ)
- **المنصة**: منصة الإعلان مع ألوان مميزة
  - 🔵 فيسبوك (Facebook)
  - 🔴 إنستغرام (Instagram)
  - 🟢 جوجل (Google)
  - 🟦 تيك توك (TikTok)
  - ⚫ تويتر (Twitter/X)
  - 🔵 لينكد إن (LinkedIn)
  - 🟡 سناب شات (Snapchat)
  - 🔲 منصات متعددة (Multiple)
- **نوع الإعلان**:
  - زيادة الوعي (Awareness)
  - زيادة الزيارات (Traffic)
  - زيادة التفاعل (Engagement)
  - عملاء محتملين (Leads)
  - زيادة المبيعات (Sales)
  - تحميل التطبيق (App Installs)
- **الميزانية**: الميزانية المخصصة
- **المدة**: مدة الحملة بالأيام
- **الحالة**:
  - ⚠️ معلق (Pending)
  - 📋 قيد المراجعة (Reviewing)
  - ✅ مقبول (Accepted)
  - ❌ مرفوض (Rejected)
  - ▶️ قيد التنفيذ (Running)
  - ✔️ مكتمل (Completed)
- **تاريخ الإنشاء**

#### المرشحات (Filters):
- تصفية حسب الحالة
- تصفية حسب المنصة

#### الإجراءات المتاحة:
- 👁️ **عرض**: عرض تفاصيل الطلب كاملة
- ✏️ **تعديل**: تعديل الطلب وتغيير الحالة
- 🗑️ **حذف**: حذف الطلب

#### شاشة التفاصيل تعرض:
- معلومات المستخدم (الاسم، البريد، الهاتف، الشركة)
- تفاصيل الإعلان (المنصة، النوع، الجمهور المستهدف)
- الميزانية والعملة
- مدة الحملة وتاريخ البدء
- محتوى الإعلان
- خيارات الاستهداف
- حالة الطلب
- ملاحظات الإدارة

---

## 🔔 شارة الإشعارات (Badge)

كل صفحة تعرض عدد الطلبات المعلقة (Pending) في شارة برتقالية بجانب اسم الصفحة في القائمة الجانبية.

---

## 📱 API Endpoints للتطبيق

### إرسال طلب موقع إلكتروني
```http
POST https://mediaprosocial.io/api/website-requests
Content-Type: application/json

{
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "phone": "+971501234567",
  "company_name": "شركة الابتكار",
  "website_type": "ecommerce",
  "description": "نريد متجر إلكتروني لبيع المنتجات",
  "budget": 5000,
  "currency": "AED",
  "deadline": "2025-12-31",
  "features": ["سلة تسوق", "بوابة دفع", "نظام عضويات"]
}
```

### إرسال طلب إعلان ممول
```http
POST https://mediaprosocial.io/api/sponsored-ad-requests
Content-Type: application/json

{
  "name": "سارة علي",
  "email": "sara@example.com",
  "phone": "+971501234567",
  "company_name": "شركة التسويق",
  "ad_platform": "facebook",
  "ad_type": "sales",
  "target_audience": "نساء من 25-45 سنة في الإمارات",
  "budget": 3000,
  "currency": "AED",
  "duration_days": 30,
  "start_date": "2025-12-01",
  "ad_content": "محتوى الإعلان...",
  "targeting_options": {
    "age_range": "25-45",
    "gender": "female",
    "location": "UAE"
  }
}
```

---

## 🔒 الأمان

- جميع endpoints الإدارية محمية بـ `auth:sanctum` middleware
- الطلبات العامة (POST) متاحة بدون مصادقة لاستقبال طلبات العملاء
- Soft Deletes مفعلة على جميع الجداول
- Validation كامل على جميع الطلبات

---

## 📈 الإحصائيات

### API للحصول على إحصائيات طلبات المواقع
```http
GET /api/admin/website-requests/statistics
```

### API للحصول على إحصائيات طلبات الإعلانات
```http
GET /api/admin/sponsored-ad-requests/statistics
```

الإحصائيات تشمل:
- إجمالي الطلبات
- عدد الطلبات حسب الحالة
- عدد الطلبات حسب المنصة (للإعلانات)

---

## 🛠️ خطوات التشغيل على السيرفر

### 1. رفع الملفات
```bash
# رفع Models
pscp -P 65002 backend/app/Models/*.php server:/path/to/app/Models/

# رفع Migrations
pscp -P 65002 backend/database/migrations/*requests*.php server:/path/to/database/migrations/

# رفع Controllers
pscp -P 65002 backend/app/Http/Controllers/Api/*Request*.php server:/path/to/app/Http/Controllers/Api/

# رفع Filament Resources
pscp -P 65002 backend/app/Filament/Resources/*Request*.php server:/path/to/app/Filament/Resources/
```

### 2. تشغيل Migrations
```bash
ssh server
cd /path/to/public_html
php artisan migrate
```

### 3. التحقق من التشغيل
```bash
php artisan migrate:status
```

---

## ✅ الخلاصة

النظام جاهز بالكامل ويتضمن:

- ✅ Models مع Relationships و Scopes
- ✅ Migrations كاملة مع Soft Deletes
- ✅ Filament Resources مع واجهة عربية كاملة
- ✅ API Controllers مع Validation
- ✅ API Routes محمية للإدارة وعامة للمستخدمين
- ✅ شارات الإشعارات للطلبات المعلقة
- ✅ مرشحات وبحث متقدم
- ✅ Infolists لعرض التفاصيل
- ✅ نظام حالات كامل (Status Management)

---

## 📞 للدعم

إذا واجهت أي مشكلة، تحقق من:
1. أن Migrations تم تطبيقها بنجاح
2. أن Filament مثبت وجاهز
3. أن API Routes مسجلة بشكل صحيح
4. أن الصلاحيات (Permissions) مضبوطة للوصول إلى لوحة التحكم

---

**تم إنشاء هذا الدليل بواسطة Claude Code**
