# دليل ميزات المحتوى والشات بوت

هذا الدليل يشرح الميزات الجديدة التي تم إضافتها لشاشة المحتوى والشات بوت الذكي.

## 📋 جدول المحتويات

- [تحسينات شاشة المحتوى](#تحسينات-شاشة-المحتوى)
- [جلب البوستات من السوشال ميديا](#جلب-البوستات-من-السوشال-ميديا)
- [الشات بوت الذكي](#الشات-بوت-الذكي)
- [مربعات المنصات المحسنة](#مربعات-المنصات-المحسنة)

---

## 🎨 تحسينات شاشة المحتوى

### الميزات الجديدة:

#### 1. **زر المساعد الذكي** 🤖
- موجود في شريط العنوان
- يفتح الشات بوت بكبسة زر
- تصميم جذاب مع تأثيرات Glow

#### 2. **زر التحديث** 🔄
- يجلب أحدث البوستات من السوشال ميديا
- مؤشر تحميل متحرك
- تحديث تلقائي عند فتح الشاشة

#### 3. **مربعات المنصات المحسنة** 🎯
كل منصة لها:
- ألوان خاصة بها
- أيقونة مميزة
- تأثير Gradient جذاب
- ظل (Shadow) ديناميكي

**المنصات المدعومة:**
- 🔵 **Facebook** - أزرق
- 🌈 **Instagram** - Gradient متعدد الألوان
- 🐦 **Twitter/X** - أزرق فاتح
- ⚫ **TikTok** - أسود وسيان
- 💼 **LinkedIn** - أزرق داكن
- 🔴 **YouTube** - أحمر

---

## 📱 جلب البوستات من السوشال ميديا

### كيفية العمل:

```dart
final fetchService = SocialMediaFetchService();

// جلب جميع البوستات
final posts = await fetchService.fetchAllPosts();

// جلب بوستات منصة معينة
final facebookPosts = await fetchService.fetchFacebookPosts();
final instagramPosts = await fetchService.fetchInstagramPosts();
final twitterPosts = await fetchService.fetchTwitterPosts();
final tiktokPosts = await fetchService.fetchTikTokPosts();
```

### المنصات المدعومة:

| المنصة | API | الحالة |
|--------|-----|--------|
| Facebook | Graph API | ✅ جاهز للتكامل |
| Instagram | Instagram API | ✅ جاهز للتكامل |
| Twitter/X | Twitter API v2 | ✅ جاهز للتكامل |
| TikTok | TikTok API | ✅ جاهز للتكامل |

### البيانات المجلوبة:

من كل بوست يتم جلب:
- 📝 المحتوى
- 🖼️ الصور/الفيديوهات
- 📅 تاريخ النشر
- ❤️ عدد الإعجابات
- 💬 عدد التعليقات
- 🔄 عدد المشاركات
- 👁️ عدد المشاهدات (للفيديو)

### الإحصائيات:

```dart
final analytics = await fetchService.getPostsAnalytics();

print('إجمالي البوستات: ${analytics['totalPosts']}');
print('إجمالي الإعجابات: ${analytics['totalLikes']}');
print('متوسط التفاعل: ${analytics['avgEngagement']}');
```

---

## 🤖 الشات بوت الذكي

### الوصول للشات بوت:

**طريقتان:**
1. من شاشة المحتوى: الضغط على أيقونة الروبوت 🤖
2. مباشرة: `Navigator.push` إلى `ChatbotScreen`

### الميزات:

#### 1. **نصيحة اليوم** 💡
- تظهر في أعلى الشاشة
- تتغير يومياً
- نصائح قيمة للسوشال ميديا

#### 2. **الاقتراحات السريعة** ⚡
عند بداية المحادثة، تظهر أزرار سريعة:
- 💡 اقترح أفكار منشورات
- ⏰ أفضل أوقات النشر
- 📝 كيف أكتب محتوى جذاب؟
- 📊 كيف أحلل أداء صفحتي؟
- #️⃣ نصائح الهاشتاجات
- 🎥 نصائح لفيديوهات ناجحة

#### 3. **ردود ذكية** 🧠

الشات بوت يفهم ويرد على:
- أسئلة عن **إنشاء المحتوى**
- أسئلة عن **الجدولة والتوقيت**
- أسئلة عن **الهاشتاجات**
- أسئلة عن **التحليلات**
- أسئلة عن **الفيديوهات**
- أسئلة عن **استراتيجيات التسويق**

#### 4. **التكامل مع AI** 🌟

يدعم نموذجين من AI:
- **Google Gemini** (الافتراضي)
- **OpenAI (ChatGPT)** (كبديل)
- **Demo Mode** (للتجربة بدون API)

### كيفية الاستخدام:

```dart
// إنشاء instance من الخدمة
final chatbot = ChatbotService();

// إرسال رسالة
final response = await chatbot.sendMessage('كيف أكتب منشور جذاب؟');
print(response.message);

// الحصول على نصيحة اليوم
final tip = chatbot.getDailyTip();
print(tip);

// الحصول على الاقتراحات السريعة
final suggestions = chatbot.getQuickSuggestions();
```

### إعداد مفاتيح AI:

في `lib/core/config/api_config.dart`:

```dart
static const String geminiApiKey = String.fromEnvironment(
  'GEMINI_API_KEY',
  defaultValue: 'YOUR_GEMINI_API_KEY',
);

static const String openAiApiKey = String.fromEnvironment(
  'OPENAI_API_KEY',
  defaultValue: 'YOUR_OPENAI_API_KEY',
);
```

أو في ملف `.env`:

```env
GEMINI_API_KEY=your_gemini_key_here
OPENAI_API_KEY=your_openai_key_here
```

---

## 🎯 مربعات المنصات المحسنة

### التصميم الجديد:

كل مربع منصة يحتوي على:

1. **Gradient مخصص** 🌈
   - ألوان رسمية للمنصة
   - انتقال سلس بين الألوان

2. **أيقونة مميزة** ✨
   - أيقونة Flutter Material
   - مناسبة لكل منصة

3. **تأثيرات بصرية** 💫
   - Shadow بلون المنصة
   - Border Radius ناعم
   - Padding مناسب

### مثال الكود:

```dart
Widget _buildEnhancedPlatformChip(String platform) {
  final platformData = _getPlatformData(platform);

  return Container(
    padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 8),
    decoration: BoxDecoration(
      gradient: LinearGradient(
        colors: platformData['colors'],
      ),
      borderRadius: BorderRadius.circular(16),
      boxShadow: [
        BoxShadow(
          color: platformData['colors'][0].withOpacity(0.4),
          blurRadius: 12,
          offset: const Offset(0, 4),
        ),
      ],
    ),
    child: Row(
      children: [
        Icon(platformData['icon'], color: Colors.white),
        const SizedBox(width: 8),
        Text(platform, style: TextStyle(color: Colors.white)),
      ],
    ),
  );
}
```

### إضافة منصة جديدة:

في `_getPlatformData`:

```dart
case 'your_platform':
  return {
    'colors': [Color(0xFF123456), Color(0xFF654321)],
    'icon': Icons.your_icon,
  };
```

---

## 📊 إحصائيات الأداء

### مؤشرات الأداء المتاحة:

```dart
final analytics = await fetchService.getPostsAnalytics();
```

**النتيجة:**
```json
{
  "totalPosts": 6,
  "totalLikes": 5513,
  "totalComments": 1001,
  "totalShares": 344,
  "totalViews": 12500,
  "avgEngagement": 1142.83
}
```

### حساب معدل التفاعل:

```
معدل التفاعل = (الإعجابات + التعليقات + المشاركات) / عدد البوستات
```

---

## 🔄 سير العمل

### عند فتح شاشة المحتوى:

```
1. تحميل الشاشة
2. عرض الأنيميشن
3. جلب البوستات من السوشال ميديا (تلقائياً)
4. عرض البوستات في Tab "المنشورة"
5. تحديث العرض
```

### عند الضغط على زر التحديث:

```
1. عرض مؤشر التحميل
2. إرسال طلبات لجميع المنصات
3. دمج النتائج وترتيبها
4. تحديث العرض
5. إخفاء مؤشر التحميل
```

### عند فتح الشات بوت:

```
1. فتح شاشة الشات بوت
2. عرض رسالة ترحيب
3. عرض نصيحة اليوم
4. عرض الاقتراحات السريعة
5. جاهز للاستقبال الرسائل
```

---

## 🎨 تخصيص التصميم

### تغيير ألوان المنصة:

```dart
case 'facebook':
  return {
    'colors': [YourCustomColor1, YourCustomColor2],
    'icon': YourCustomIcon,
  };
```

### تخصيص رسائل الشات بوت:

في `chatbot_service.dart` > `_generateDemoResponse`:

```dart
if (message.contains('your_keyword')) {
  return 'Your custom response';
}
```

### تغيير النصائح اليومية:

في `getDailyTip`:

```dart
final tips = [
  'نصيحتك الأولى',
  'نصيحتك الثانية',
  // ...
];
```

---

## 🚀 الاستخدام المتقدم

### جلب بوستات مخصصة:

```dart
final posts = await fetchService.fetchAllPosts();

// تصفية حسب المنصة
final fbOnly = posts.where((p) => p.platforms.contains('Facebook'));

// تصفية حسب التاريخ
final recent = posts.where((p) =>
  p.publishedAt?.isAfter(DateTime.now().subtract(Duration(days: 7))) ?? false
);

// تصفية حسب التفاعل
final popular = posts.where((p) =>
  (p.analytics?['likes'] ?? 0) > 500
);
```

### دمج مع الإشعارات:

```dart
// عند جلب بوست جديد
if (newPost.analytics!['likes']! > 1000) {
  await NotificationService().showNotification(
    title: 'إنجاز جديد!',
    body: 'حصلت على أكثر من 1000 إعجاب!',
  );
}
```

---

## 📝 ملاحظات مهمة

### الأمان:
- 🔒 لا تضع API Keys في الكود مباشرة
- 🔐 استخدم ملف `.env` للمفاتيح
- 🛡️ أضف `.env` إلى `.gitignore`

### الأداء:
- ⚡ البوستات يتم cache محلياً
- 🔄 التحديث يكون lazy (عند الحاجة)
- 💾 استخدام Hive للتخزين المحلي

### التوافق:
- ✅ Android
- ✅ iOS
- ✅ Web (مع قيود APIs)

---

## 🆘 المشاكل الشائعة

### 1. البوستات لا تظهر؟
- ✅ تأكد من اتصالك بالإنترنت
- ✅ تحقق من صحة API Keys
- ✅ راجع Console للأخطاء

### 2. الشات بوت لا يرد؟
- ✅ تحقق من مفتاح Gemini/OpenAI
- ✅ استخدم Demo Mode للاختبار
- ✅ راجع الأخطاء في Console

### 3. مربعات المنصات تظهر بألوان خاطئة؟
- ✅ تأكد من اسم المنصة (Case Sensitive)
- ✅ أضف المنصة في `_getPlatformData`

---

## 🎓 تعلم المزيد

### موارد مفيدة:
- [Facebook Graph API](https://developers.facebook.com/docs/graph-api/)
- [Instagram API](https://developers.facebook.com/docs/instagram-api)
- [Twitter API](https://developer.twitter.com/en/docs)
- [TikTok API](https://developers.tiktok.com/)
- [Google Gemini](https://ai.google.dev/)
- [OpenAI API](https://platform.openai.com/docs)

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع هذا الدليل
2. تحقق من الـ Console للأخطاء
3. راجع الكود المصدري
4. اتصل بفريق التطوير

---

تم التطوير بواسطة فريق Social Media Manager 🚀
آخر تحديث: 2025-01-04
