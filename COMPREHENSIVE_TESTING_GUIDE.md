# 🧪 دليل اختبار التطبيق الشامل

**الهدف:** اختبار جميع وظائف التطبيق والتحقق من حفظ البيانات في قاعدة البيانات  
**المدة:** ~30 دقيقة  
**النتيجة المتوقعة:** ✅ جميع البيانات محفوظة بنجاح

---

## 📋 قائمة الاختبارات

### Part 1: المصادقة والدخول (5 دقائق)

#### ✅ اختبار 1.1: تسجيل مستخدم جديد بـ OTP
```
الخطوات:
1. افتح التطبيق
2. انقر على "تسجيل جديد" أو "ليس لديك حساب"
3. أدخل رقم الهاتف: +201001234567
4. انقر "إرسال رمز"
5. استقبل الرمز على هاتفك
6. أدخل الرمز
7. أدخل اسمك وبريدك (اختياري)
8. انقر "متابعة"

النتيجة المتوقعة:
✅ تم الدخول بنجاح
✅ انتقلت إلى الشاشة الرئيسية
✅ ظهرت بيانات المستخدم

التحقق من قاعدة البيانات:
- اذهب إلى phpMyAdmin
- Database: u126213189_socialmedia_ma
- جدول: users
- ابحث عن رقم الهاتف +201001234567
  ✅ يجب أن تجد صف جديد مع:
     - phone_number: +201001234567
     - name: (الاسم الذي أدخلته)
     - is_verified: 1
     - user_type: individual
```

#### ✅ اختبار 1.2: تسجيل الدخول بحساب موجود
```
الخطوات:
1. من شاشة تسجيل الدخول
2. أدخل رقم الهاتف الذي استخدمته للتو
3. انقر "إرسال رمز"
4. أدخل الرمز
5. انقر "دخول"

النتيجة المتوقعة:
✅ تم الدخول بنجاح
✅ ظهرت البيانات المحفوظة السابقة

التحقق من قاعدة البيانات:
- جدول: users
- ابحث عن نفس المستخدم
  ✅ يجب أن يكون: last_login محدث بالوقت الحالي
  ✅ جدول: sessions
     - يجب أن يكون هناك صف جديد مع user_id
     - ip_address: عنوان IP الحالي
     - last_activity: الوقت الحالي
```

---

### Part 2: إدارة وسائل التواصل الاجتماعي (10 دقائق)

#### ✅ اختبار 2.1: عرض الحسابات المتصلة (أو فارغ إذا كان جديد)
```
الخطوات:
1. من الشاشة الرئيسية
2. انقر على "حساباتي" أو "وسائل التواصل"
3. انقر على "الحسابات المتصلة"

النتيجة المتوقعة:
✅ ظهرت قائمة الحسابات المتصلة (قد تكون فارغة للمستخدمين الجدد)

التحقق من قاعدة البيانات:
- جدول: connected_accounts
- عرّف المستخدم: WHERE user_id = 1 (أو معرف المستخدم الصحيح)
  ✅ إذا كان هناك حسابات قديمة:
     - platform: facebook/instagram/twitter/linkedin
     - account_id: معرف الحساب
     - is_active: 1
```

#### ✅ اختبار 2.2: ربط حساب Facebook جديد
```
الخطوات:
1. من قائمة الحسابات المتصلة
2. انقر "ربط حساب جديد"
3. اختر "Facebook"
4. انقر "تسجيل الدخول عبر Facebook"
5. في نافذة Facebook: أدخل بيانات حساب اختبار
6. وافق على الصلاحيات
7. قد يطلب التطبيق "الصفحة" - اختر صفحة موجودة

النتيجة المتوقعة:
✅ تم ربط الحساب بنجاح
✅ ظهر الحساب في القائمة مع:
   - الاسم
   - الصورة
   - حالة (مفعل)

التحقق من قاعدة البيانات:
- جدول: connected_accounts
- ابحث عن: WHERE platform = 'facebook' AND user_id = 1
  ✅ يجب أن تجد صف جديد:
     - account_id: رقم الصفحة من Facebook
     - account_name: اسم الصفحة
     - access_token: التوكن الآمن
     - is_active: 1
     - created_at: الوقت الحالي

✅ جدول: api_logs
  ✅ يجب أن تجد طلب API جديد:
     - endpoint: /social-accounts
     - method: POST
     - user_id: 1
     - status_code: 201 أو 200
```

#### ✅ اختبار 2.3: ربط Instagram (اختياري)
```
نفس الخطوات لـ Facebook لكن مع Instagram

ملاحظة:
- Instagram يحتاج Business Account
- أو تحويل Personal Account إلى Business
- الرمز قد يستغرق بعض الدقائق للتفعيل
```

---

### Part 3: المحتوى والنشر (10 دقائق)

#### ✅ اختبار 3.1: إنشاء منشور جديد
```
الخطوات:
1. من الشاشة الرئيسية
2. انقر على "محتوى جديد" أو "إنشاء منشور"
3. اكتب محتوى: "هذا اختبار لحفظ البيانات 🎉"
4. (اختياري) أضف صورة أو فيديو
5. حدد المنصات: اختر Facebook و Instagram

النتيجة المتوقعة:
✅ تم إنشاء المنشور

التحقق من قاعدة البيانات:
- جدول: auto_scheduled_posts
  ✅ يجب أن تجد صف جديد:
     - user_id: 1
     - content: "هذا اختبار لحفظ البيانات 🎉"
     - platforms: ["facebook", "instagram"]
     - status: "draft" أو "pending"
     - created_at: الوقت الحالي
```

#### ✅ اختبار 3.2: جدولة منشور (نشر في وقت لاحق)
```
الخطوات:
1. من إنشاء منشور جديد
2. اكتب محتوى: "منشور مجدول"
3. انقر على "جدولة النشر"
4. اختر الوقت: الساعة 3 مساءً من غد
5. انقر "حفظ"

النتيجة المتوقعة:
✅ تم جدولة المنشور

التحقق من قاعدة البيانات:
- جدول: auto_scheduled_posts
  ✅ يجب أن تجد صف جديد:
     - scheduled_time: تاريخ ووقت النشر المجدول
     - status: "scheduled"
     - created_at: الآن
```

#### ✅ اختبار 3.3: النشر الفوري
```
الخطوات:
1. من إنشاء منشور
2. اكتب محتوى: "هذا منشور فوري"
3. تأكد أن لديك حساب Facebook مربوط
4. انقر "نشر الآن"

النتيجة المتوقعة:
✅ ظهرت رسالة "جاري النشر..."
✅ بعد دقيقة: "تم النشر بنجاح"
✅ المنشور ظهر على Facebook الفعلي

التحقق من قاعدة البيانات:
- جدول: auto_scheduled_posts
  ✅ يجب أن تجد الصف:
     - status: "posted"
     - published_at: الوقت الذي تم النشر فيه

- جدول: api_logs
  ✅ يجب أن تجد طلبات:
     - endpoint: /posts أو /social-accounts/*/post
     - status_code: 201 أو 200
```

---

### Part 4: الاشتراكات والدفع (5 دقائق)

#### ✅ اختبار 4.1: عرض خطط الاشتراك
```
الخطوات:
1. من الشاشة الرئيسية
2. انقر على "الاشتراكات" أو "المزايا"
3. انقر على "ترقية الخطة"

النتيجة المتوقعة:
✅ ظهرت قائمة خطط الاشتراك:
   - Starter Plan (مجاني)
   - Professional (150 جنيه/شهر)
   - Enterprise (500 جنيه/شهر)

التحقق من قاعدة البيانات:
- جدول: subscription_plans
  ✅ يجب أن تجد:
     - tier: professional
     - monthly_price: 150
     - currency: EGP
     - features: [...]

- جدول: api_logs
  ✅ يجب أن تجد طلب:
     - endpoint: /api/subscription-plans
     - method: GET
     - status_code: 200
```

#### ✅ اختبار 4.2: الاشتراك في خطة (يحتاج مفتاح Paymob صحيح)
```
الخطوات:
1. من قائمة الخطط
2. انقر على "Professional Plan"
3. انقر على "اشتراك الآن"
4. سيتم فتح نموذج الدفع عبر Paymob

⚠️ ملاحظة: إذا ظهرت رسالة خطأ:
"خطأ في المصادقة مع Paymob"
→ هذا يعني أن مفتاح API غير صحيح
→ تحتاج إلى تحديثه في: lib/core/config/api_config.dart

الحل السريع:
1. اذهب إلى: https://accept.paymob.com/portal2/en/settings
2. انسخ API Key الصحيح
3. استبدل في api_config.dart
4. أعد بناء التطبيق: flutter clean && flutter pub get && flutter run

التحقق من قاعدة البيانات (بعد إصلاح المفتاح):
- جدول: subscriptions
  ✅ يجب أن تجد صف جديد:
     - user_id: 1
     - plan_id: 2 (Professional)
     - status: "active"
     - start_date: اليوم
     - end_date: بعد 30 يوم

- جدول: payments
  ✅ يجب أن تجد صف جديد:
     - user_id: 1
     - amount: 150
     - currency: EGP
     - payment_gateway: paymob
     - transaction_id: رقم المعاملة من Paymob
     - status: "completed"

- جدول: api_logs
  ✅ يجب أن تجد طلبات:
     - endpoint: /subscriptions
     - method: POST
     - status_code: 201
```

---

### Part 5: التحليلات والإحصائيات (3 دقائق)

#### ✅ اختبار 5.1: عرض التحليلات
```
الخطوات:
1. من الشاشة الرئيسية
2. انقر على "التحليلات" أو "الإحصائيات"
3. يجب أن ترى:
   - عدد المتابعين
   - عدد الإعجابات
   - معدل الانخراط
   - أفضل المنشورات

النتيجة المتوقعة:
✅ تم تحميل البيانات من المنصات المتصلة

التحقق من قاعدة البيانات:
- جدول: api_logs
  ✅ يجب أن تجد طلبات:
     - endpoint: /analytics أو /stats
     - method: GET
     - status_code: 200
```

---

### Part 6: الإعدادات والملف الشخصي (2 دقيقة)

#### ✅ اختبار 6.1: تحديث الملف الشخصي
```
الخطوات:
1. من الشاشة الرئيسية
2. انقر على "الإعدادات" أو الصورة الشخصية
3. انقر "تعديل الملف الشخصي"
4. عدّل البيانات:
   - الاسم: "أحمد محمد"
   - البريد: "ahmed@example.com"
   - الصورة الشخصية: اختر صورة
5. انقر "حفظ"

النتيجة المتوقعة:
✅ ظهرت رسالة "تم الحفظ بنجاح"
✅ البيانات تحدثت فوراً

التحقق من قاعدة البيانات:
- جدول: users
  ✅ يجب أن تجد الصف مع:
     - name: "أحمد محمد"
     - email: "ahmed@example.com"
     - avatar_url: رابط الصورة
     - updated_at: الوقت الحالي
```

---

## 🔍 كيفية التحقق من قاعدة البيانات

### الطريقة 1: استخدام phpMyAdmin
```
1. اذهب إلى: https://mediaprosocial.io/phpmyadmin
2. أدخل:
   - Username: u126213189
   - Password: Alenwanapp33510421@
3. اختر Database: u126213189_socialmedia_ma
4. استكشف الجداول المذكورة أعلاه
```

### الطريقة 2: استخدام Command Line
```bash
# من الخادم:
mysql -h 82.25.83.217 -u u126213189 -p u126213189_socialmedia_ma

# ثم:
SELECT * FROM users WHERE phone_number = '+201001234567';
SELECT * FROM connected_accounts WHERE user_id = 1;
SELECT * FROM auto_scheduled_posts WHERE user_id = 1;
SELECT * FROM subscriptions WHERE user_id = 1;
SELECT * FROM api_logs WHERE user_id = 1 ORDER BY created_at DESC LIMIT 10;
```

### الطريقة 3: استخدام Laravel Admin Panel
```
1. اذهب إلى: https://mediaprosocial.io/admin
2. أدخل بيانات المسؤول
3. انقر على "Users" لرؤية جميع المستخدمين
4. انقر على "API Logs" لرؤية جميع الطلبات
5. انقر على "Posts" لرؤية المنشورات المحفوظة
```

---

## ✅ قائمة التحقق الأخيرة

### بعد إكمال جميع الاختبارات:

- ✅ تم تسجيل دخول مستخدم جديد → البيانات محفوظة في `users`
- ✅ تم ربط حساب Facebook → البيانات محفوظة في `connected_accounts`
- ✅ تم إنشاء منشور → البيانات محفوظة في `auto_scheduled_posts`
- ✅ تم نشر منشور → الحالة تحدثت إلى "posted"
- ✅ تم عرض الخطط → طلب API تم تسجيله في `api_logs`
- ✅ تم الاشتراك (بعد إصلاح مفتاح Paymob) → البيانات محفوظة في `subscriptions` و `payments`
- ✅ تم تحديث الملف الشخصي → البيانات محفوظة في `users`
- ✅ جميع الطلبات تم تسجيلها → كل طلب في `api_logs`

---

## 🎯 النتيجة النهائية

```
✅ جميع وظائف التطبيق تعمل بشكل صحيح
✅ جميع البيانات محفوظة في قاعدة البيانات
✅ النوافذ المنبثقة توضح حالة العمليات
✅ التطبيق يتعامل مع الأخطاء بشكل صحيح
✅ البيانات آمنة ومشفرة

التطبيق جاهز للإطلاق الحقيقي! 🚀
```

---

## 📞 في حالة وجود مشاكل

### ❌ المشكلة: "خطأ في الاتصال بالخادم"
**الحل:**
- تحقق من اتصال الإنترنت
- تأكد من أن عنوان الخادم صحيح في `api_config.dart`
- جرب VPN إذا كان الخادم محدود جغرافياً

### ❌ المشكلة: "خطأ في المصادقة"
**الحل:**
- تحقق من أن التوكن محفوظ بشكل صحيح
- حاول تسجيل الخروج ثم الدخول مرة أخرى
- امسح بيانات التطبيق وأعد التشغيل

### ❌ المشكلة: "خطأ في Paymob (403)"
**الحل:**
- تحديث مفتاح API من https://accept.paymob.com/portal2/en/settings
- تأكد من أن المفتاح ليس منتهي الصلاحية

### ❌ المشكلة: "لا تظهر البيانات في قاعدة البيانات"
**الحل:**
- تحقق من الاتصال بقاعدة البيانات
- تأكد من أن جميع الـ Migrations تم تشغيلها
- تحقق من سجلات الخادم: `storage/logs/laravel.log`

---

**تم إعداد هذا الدليل:** 2025  
**النسخة:** 1.0
