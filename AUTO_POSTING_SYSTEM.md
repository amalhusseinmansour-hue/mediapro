# نظام النشر التلقائي (Auto-Posting System)

## 🎯 نظرة عامة

تم إنشاء نظام متكامل للنشر التلقائي على منصات التواصل الاجتماعي مع:
- جدولة ذكية للمنشورات
- تكرار المنشورات (يومي، أسبوعي، شهري، مخصص)
- إدارة متعددة المنصات
- نظام إيقاف مؤقت واستئناف
- تتبع حالة المنشورات

---

## 📋 التغييرات في الكود

### 1. تم إزالة من شاشة الحسابات
✅ تم إزالة قسم "الحسابات المتصلة" (Connected Accounts Section)
- السطور 435-471 تم حذفها
- الشاشة الآن تحتوي فقط على:
  - إحصائيات الحسابات
  - ربط حسابات جديدة
  - جدولة المنشورات
  - المنصات المتاحة للربط

---

## 🚀 المكونات الجديدة

### Backend (Laravel)

#### 1. Model: `AutoScheduledPost`
**المسار:** `backend/app/Models/AutoScheduledPost.php`

**الحقول:**
```php
- user_id: معرف المستخدم
- content: محتوى المنشور
- media_urls: روابط الصور/الفيديوهات (JSON)
- platforms: المنصات المستهدفة (JSON)
- schedule_time: وقت النشر الأول
- recurrence_pattern: نمط التكرار (once, daily, weekly, monthly, custom)
- recurrence_interval: فترة التكرار (للنمط المخصص)
- recurrence_end_date: تاريخ انتهاء التكرار
- is_active: حالة التفعيل
- status: الحالة (pending, active, paused, completed, failed)
- last_posted_at: آخر وقت نشر
- next_post_at: وقت النشر القادم
- post_count: عدد المرات التي تم النشر فيها
- metadata: بيانات إضافية (JSON)
```

**الدوال الرئيسية:**
- `activate()`: تفعيل الجدولة
- `pause()`: إيقاف مؤقت
- `complete()`: إكمال الجدولة
- `calculateNextPostTime()`: حساب وقت النشر القادم
- `markAsPosted()`: تسجيل عملية نشر
- `shouldPost()`: التحقق إذا حان وقت النشر

#### 2. Migration
**المسار:** `backend/database/migrations/2025_11_10_000002_create_auto_scheduled_posts_table.php`

---

## 🎨 أنماط التكرار

### 1. مرة واحدة (Once)
- ينشر المنشور مرة واحدة في الوقت المحدد
- يتم تعيين الحالة إلى "completed" بعد النشر

### 2. يومي (Daily)
- ينشر المنشور كل يوم/أيام
- `recurrence_interval`: عدد الأيام بين كل منشور

### 3. أسبوعي (Weekly)
- ينشر المنشور كل أسبوع/أسابيع
- `recurrence_interval`: عدد الأسابيع بين كل منشور

### 4. شهري (Monthly)
- ينشر المنشور كل شهر/أشهر
- `recurrence_interval`: عدد الأشهر بين كل منشور

### 5. مخصص (Custom)
- ينشر المنشور حسب عدد الساعات المحددة
- `recurrence_interval`: عدد الساعات بين كل منشور

---

## 📱 واجهة المستخدم (Flutter)

### الشاشات المطلوب إضافتها:

#### 1. شاشة إعدادات الأوتوميشن
**المسار المقترح:** `lib/screens/automation/automation_settings_screen.dart`

**المميزات:**
- إنشاء جدولة جديدة
- اختيار المحتوى
- رفع الصور/الفيديوهات
- اختيار المنصات المستهدفة
- تحديد وقت النشر الأول
- اختيار نمط التكرار
- تحديد فترة التكرار
- تحديد تاريخ الانتهاء (اختياري)

#### 2. شاشة المنشورات المجدولة
**المسار المقترح:** `lib/screens/automation/automated_posts_screen.dart`

**المميزات:**
- عرض جميع المنشورات المجدولة
- فلترة حسب الحالة (نشط، متوقف، مكتمل)
- عرض وقت النشر القادم
- عرض عدد مرات النشر
- إيقاف/تفعيل الجدولة
- تعديل الجدولة
- حذف الجدولة

---

## 🔧 خطوات التنفيذ المتبقية

### Backend

#### 1. إنشاء Controller
**المسار:** `backend/app/Http/Controllers/Api/AutoScheduledPostController.php`

```php
- store(): إنشاء جدولة جديدة
- index(): جلب جميع الجدولات
- show($id): تفاصيل جدولة محددة
- update($id): تعديل جدولة
- delete($id): حذف جدولة
- activate($id): تفعيل جدولة
- pause($id): إيقاف مؤقت
- getDueForPosting(): جلب المنشورات المستحقة للنشر
```

#### 2. إضافة Routes في `api.php`
```php
Route::prefix('auto-scheduled-posts')->group(function () {
    Route::get('/', [AutoScheduledPostController::class, 'index']);
    Route::post('/', [AutoScheduledPostController::class, 'store']);
    Route::get('/{id}', [AutoScheduledPostController::class, 'show']);
    Route::put('/{id}', [AutoScheduledPostController::class, 'update']);
    Route::delete('/{id}', [AutoScheduledPostController::class, 'delete']);
    Route::post('/{id}/activate', [AutoScheduledPostController::class, 'activate']);
    Route::post('/{id}/pause', [AutoScheduledPostController::class, 'pause']);
    Route::get('/due/posting', [AutoScheduledPostController::class, 'getDueForPosting']);
});
```

#### 3. إنشاء Cron Job / Scheduler
**المسار:** `backend/app/Console/Commands/ProcessAutoScheduledPosts.php`

```php
// يعمل كل دقيقة للتحقق من المنشورات المستحقة للنشر
- جلب جميع المنشورات المستحقة (shouldPost())
- نشر على كل منصة
- تسجيل عملية النشر (markAsPosted())
- حساب وقت النشر القادم
```

#### 4. Filament Resource
**المسار:** `backend/app/Filament/Resources/AutoScheduledPostResource.php`

لوحة تحكم للأدمن لإدارة جميع المنشورات المجدولة:
- عرض جميع الجدولات
- فلترة حسب الحالة
- عرض الإحصائيات
- تفعيل/إيقاف الجدولات
- حذف الجدولات

---

### Flutter

#### 1. Model
**المسار:** `lib/models/auto_scheduled_post.dart`

```dart
class AutoScheduledPost {
  final int id;
  final String userId;
  final String content;
  final List<String> mediaUrls;
  final List<String> platforms;
  final DateTime scheduleTime;
  final String recurrencePattern;
  final int? recurrenceInterval;
  final DateTime? recurrenceEndDate;
  final bool isActive;
  final String status;
  final DateTime? lastPostedAt;
  final DateTime? nextPostAt;
  final int postCount;

  // من JSON
  // إلى JSON
}
```

#### 2. Service
**المسار:** `lib/services/auto_posting_service.dart`

```dart
class AutoPostingService extends GetxService {
  // إنشاء جدولة جديدة
  Future<Map<String, dynamic>> createAutoPost({...});

  // جلب جميع الجدولات
  Future<void> getAutoPosts();

  // تفعيل/إيقاف
  Future<bool> activatePost(int id);
  Future<bool> pausePost(int id);

  // تعديل
  Future<bool> updatePost(int id, {...});

  // حذف
  Future<bool> deletePost(int id);
}
```

---

## 📊 حالات المنشور (Status)

| الحالة | الوصف | اللون المقترح |
|--------|-------|---------------|
| pending | قيد الانتظار - لم يبدأ بعد | 🟡 أصفر |
| active | نشط - يعمل التلقائي | 🟢 أخضر |
| paused | متوقف مؤقتاً | 🟠 برتقالي |
| completed | مكتمل - انتهى التكرار | 🔵 أزرق |
| failed | فشل - حدث خطأ | 🔴 أحمر |

---

## 🎯 مثال على سيناريو الاستخدام

### المستخدم يريد:
"نشر اقتباس يومي على Facebook وInstagram كل يوم الساعة 9 صباحاً لمدة 30 يوم"

### الإعدادات:
```dart
{
  "content": "اقتباس اليوم: ...",
  "platforms": ["facebook", "instagram"],
  "schedule_time": "2025-11-10 09:00:00",
  "recurrence_pattern": "daily",
  "recurrence_interval": 1,
  "recurrence_end_date": "2025-12-10 09:00:00",
  "is_active": true,
  "status": "active"
}
```

### ما يحدث:
1. اليوم الأول: ينشر الساعة 9 صباحاً
2. يحسب الوقت القادم: غداً الساعة 9 صباحاً
3. يكرر يومياً لمدة 30 يوم
4. بعد 30 يوم: يتوقف تلقائياً (status = completed)

---

## 🔐 الأمان والصلاحيات

- كل مستخدم يرى جدولاته فقط
- الأدمن يرى جميع الجدولات
- التحقق من حدود الاشتراك قبل الإنشاء
- التحقق من صلاحية الوصول للمنصات

---

## 📝 قائمة المهام

### Backend
- [ ] إنشاء AutoScheduledPostController
- [ ] إضافة Routes في api.php
- [ ] إنشاء Cron Job
- [ ] إنشاء Filament Resource
- [ ] تشغيل Migration

### Flutter
- [ ] إنشاء Model
- [ ] إنشاء Service
- [ ] إضافة شاشة إعدادات الأوتوميشن
- [ ] إضافة شاشة المنشورات المجدولة
- [ ] تعديل شاشة الحسابات (✅ تم)

### تكامل
- [ ] ربط مع Upload-Post API
- [ ] ربط مع منصات التواصل
- [ ] اختبار النظام كامل

---

## 🎨 تصميم UI المقترح

### شاشة إعدادات الأوتوميشن
```
┌─────────────────────────────────────┐
│  إنشاء جدولة نشر تلقائي            │
├─────────────────────────────────────┤
│  📝 محتوى المنشور                   │
│  [____________TextArea____________]  │
│                                      │
│  🖼️  الصور/الفيديوهات                │
│  [+ إضافة ملفات]                    │
│                                      │
│  🌐 المنصات المستهدفة                │
│  ☑ Facebook  ☑ Instagram             │
│  ☐ Twitter   ☐ LinkedIn              │
│                                      │
│  ⏰ وقت النشر الأول                  │
│  [📅 التاريخ]  [🕐 الوقت]           │
│                                      │
│  🔄 نمط التكرار                      │
│  ⚪ مرة واحدة                         │
│  ⚫ يومي (كل ___ يوم)                │
│  ⚪ أسبوعي (كل ___ أسبوع)            │
│  ⚪ شهري (كل ___ شهر)                │
│  ⚪ مخصص (كل ___ ساعة)               │
│                                      │
│  📅 تاريخ الانتهاء (اختياري)         │
│  [____________Date_____________]     │
│                                      │
│  [   إنشاء الجدولة   ]              │
└─────────────────────────────────────┘
```

### شاشة المنشورات المجدولة
```
┌─────────────────────────────────────┐
│  المنشورات المجدولة تلقائياً        │
├─────────────────────────────────────┤
│  [الكل] [نشط] [متوقف] [مكتمل]      │
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │ 🟢 نشط | يومي                 │  │
│  │ "اقتباس اليوم: ..."            │  │
│  │ 📱 Facebook, Instagram          │  │
│  │ ⏰ القادم: غداً 9:00 ص         │  │
│  │ 📊 تم النشر 5 مرات             │  │
│  │ [⏸️ إيقاف] [✏️ تعديل] [🗑️ حذف]  │  │
│  └───────────────────────────────┘  │
│                                      │
│  ┌───────────────────────────────┐  │
│  │ 🟠 متوقف | أسبوعي              │  │
│  │ "منشور أسبوعي"                 │  │
│  │ 📱 Facebook                     │  │
│  │ ⏰ القادم: الأحد 10:00 ص       │  │
│  │ 📊 تم النشر 3 مرات             │  │
│  │ [▶️ تفعيل] [✏️ تعديل] [🗑️ حذف]  │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## 🚀 الخطوات القادمة

1. **مراجعة التصميم**: تأكد من أن النظام يلبي احتياجاتك
2. **تنفيذ Backend**: إكمال Controller + Cron Job
3. **تنفيذ Flutter**: إنشاء الشاشات والخدمات
4. **الاختبار**: اختبار شامل للنظام
5. **النشر**: رفع على السيرفر

---

## 📞 ملاحظات مهمة

⚠️ **Cron Job ضروري**: يجب تشغيل Cron Job كل دقيقة للتحقق من المنشورات المستحقة
⚠️ **صلاحيات API**: تأكد من صلاحيات الوصول لمنصات التواصل
⚠️ **حدود الاشتراك**: تحقق من حدود الاشتراك قبل إنشاء جدولات
⚠️ **الأخطاء**: سجل الأخطاء في metadata لتتبع المشاكل

---

تم التحديث: 2025-11-10
النسخة: 1.0.0
