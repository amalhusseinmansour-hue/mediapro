# دليل تكامل الذكاء الاصطناعي

هذا الدليل يشرح كيفية استخدام وتكوين خدمات توليد الصور والفيديوهات بالذكاء الاصطناعي في التطبيق.

## 📋 جدول المحتويات

- [النماذج المتاحة](#النماذج-المتاحة)
- [إعداد المفاتيح](#إعداد-المفاتيح)
- [توليد الصور](#توليد-الصور)
- [توليد الفيديوهات](#توليد-الفيديوهات)
- [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## 🤖 النماذج المتاحة

### نماذج توليد الصور

1. **Nano Banana AI** ⭐ (الافتراضي)
   - نموذج متقدم لتوليد الصور
   - سريع وعالي الجودة
   - يدعم نماذج Flux وغيرها

2. **Stability AI**
   - Stable Diffusion الشهير
   - جودة عالية ودقة ممتازة
   - يدعم خيارات متقدمة

3. **Demo Mode**
   - وضع تجريبي للاختبار
   - لا يحتاج إلى API key
   - يستخدم صور placeholder

### نماذج توليد الفيديو

1. **Google Veo** ⭐ (الافتراضي)
   - نموذج Google المتقدم لتوليد الفيديوهات
   - جودة عالية حتى 1080p
   - يدعم فيديوهات حتى 60 ثانية

2. **D-ID**
   - توليد فيديوهات بمتحدث افتراضي
   - يدعم اللغة العربية والإنجليزية
   - مناسب للفيديوهات التعليمية

3. **Synthesia**
   - فيديوهات احترافية بمقدمين افتراضيين
   - متعدد اللغات
   - جودة إنتاج عالية

4. **Demo Mode**
   - وضع تجريبي للاختبار
   - لا يحتاج إلى API key

---

## 🔑 إعداد المفاتيح

### 1. احصل على مفاتيح API

#### Nano Banana
1. زر [https://nanobanana.ai](https://nanobanana.ai)
2. أنشئ حساب
3. انسخ API Key من لوحة التحكم

#### Stability AI
1. زر [https://platform.stability.ai](https://platform.stability.ai)
2. أنشئ حساب
3. احصل على API Key

#### Google Veo
1. زر [https://ai.google.dev](https://ai.google.dev)
2. أنشئ مشروع جديد
3. فعّل Veo API
4. احصل على API Key

#### D-ID
1. زر [https://www.d-id.com](https://www.d-id.com)
2. أنشئ حساب
3. احصل على API Key

### 2. إضافة المفاتيح إلى التطبيق

#### الطريقة الأولى: ملف .env (موصى بها)

1. انسخ ملف `.env.example` إلى `.env`:
```bash
copy .env.example .env
```

2. افتح ملف `.env` وأضف مفاتيحك:
```env
NANO_BANANA_API_KEY=your_actual_key_here
STABILITY_AI_API_KEY=your_actual_key_here
GOOGLE_VEO_API_KEY=your_actual_key_here
DID_API_KEY=your_actual_key_here
```

3. تأكد من إضافة `.env` إلى `.gitignore`

#### الطريقة الثانية: تعديل ملف التكوين

افتح `lib/core/config/api_config.dart` وعدّل المفاتيح مباشرة:

```dart
static const String nanoBananaApiKey = 'your_key_here';
```

⚠️ **تحذير**: لا ترفع المفاتيح إلى Git!

---

## 🖼️ توليد الصور

### استخدام الخدمة

```dart
import 'package:social_media_manager/services/ai_image_service.dart';

final imageService = AIImageService();

// اختيار النموذج
imageService.setModel(ImageGenerationModel.nanoBanana);

// توليد صورة
final image = await imageService.generateImage(
  prompt: 'صورة واقعية لغروب شمس جميل على الشاطئ',
  negativePrompt: 'ضبابي، سيئ الجودة',
  width: 1024,
  height: 1024,
  steps: 30,
  cfgScale: 7.0,
);

print('تم توليد الصورة: ${image.imageUrl}');
```

### المعاملات المتاحة

| المعامل | الوصف | القيمة الافتراضية |
|---------|-------|-------------------|
| `prompt` | وصف الصورة المطلوبة | مطلوب |
| `negativePrompt` | ما لا تريده في الصورة | null |
| `width` | عرض الصورة (بكسل) | 512 |
| `height` | ارتفاع الصورة (بكسل) | 512 |
| `steps` | عدد خطوات التوليد | 30 |
| `cfgScale` | قوة الالتزام بالوصف (1-20) | 7.0 |
| `seed` | Seed للتحكم في النتيجة | null |
| `model` | النموذج المستخدم | nanoBanana |

### أحجام الصور المدعومة

- 512×512 (مربع صغير)
- 768×768 (مربع متوسط)
- 1024×1024 (مربع كبير)
- 512×768 (عمودي)
- 768×512 (أفقي)

---

## 🎬 توليد الفيديوهات

### استخدام الخدمة

```dart
import 'package:social_media_manager/services/ai_video_service.dart';

final videoService = AIVideoService();

// اختيار النموذج
videoService.setModel(VideoGenerationModel.googleVeo);

// 1. توليد سكربت
final script = await videoService.generateScript(
  topic: 'نصائح لتحسين الإنتاجية',
  description: 'فيديو تعليمي قصير',
  duration: 60,
  language: 'ar',
  videoType: 'educational',
);

// 2. توليد فيديو من السكربت
final video = await videoService.generateVideo(
  scriptId: script.id,
  duration: 60,
  quality: '1080p',
);

print('تم توليد الفيديو: ${video.videoUrl}');
```

### التوليد المباشر

يمكنك أيضًا توليد فيديو مباشرة من نص:

```dart
final video = await videoService.generateVideo(
  prompt: 'فيديو قصير عن غروب الشمس على الشاطئ',
  duration: 5,
  quality: '720p',
  model: VideoGenerationModel.googleVeo,
);
```

### المعاملات المتاحة

| المعامل | الوصف | القيمة الافتراضية |
|---------|-------|-------------------|
| `scriptId` | معرف السكربت | null |
| `prompt` | وصف الفيديو المطلوب | null |
| `duration` | مدة الفيديو (ثواني) | 5 |
| `quality` | جودة الفيديو | 720p |
| `model` | النموذج المستخدم | googleVeo |

### جودة الفيديو المدعومة

- `480p` - جودة منخفضة
- `720p` - HD
- `1080p` - Full HD

---

## ❓ الأسئلة الشائعة

### كيف أتحقق من صحة مفاتيح API؟

```dart
import 'package:social_media_manager/core/config/api_config.dart';

// طباعة حالة جميع الخدمات
ApiConfig.printServicesStatus();

// التحقق من خدمة معينة
if (ApiConfig.isServiceAvailable('nano_banana')) {
  print('Nano Banana متاح!');
}
```

### ما هي تكلفة استخدام هذه الخدمات؟

كل خدمة لها نموذج تسعير مختلف:
- **Nano Banana**: يبدأ من $0.02 لكل صورة
- **Stability AI**: $0.002-0.01 لكل صورة
- **Google Veo**: يختلف حسب المدة والجودة
- **D-ID**: $0.05-0.30 لكل فيديو

### كم يستغرق توليد الصورة/الفيديو؟

- **الصور**: 3-10 ثواني
- **الفيديوهات**: 30 ثانية - 5 دقائق

### هل يمكنني استخدام التطبيق بدون مفاتيح API؟

نعم! يمكنك استخدام وضع Demo للاختبار:

```dart
imageService.setModel(ImageGenerationModel.demo);
videoService.setModel(VideoGenerationModel.demo);
```

### كيف أحفظ الصور المولدة؟

```dart
final image = await imageService.generateImage(prompt: '...');

// الصورة محفوظة في:
print(image.imageUrl);

// يمكنك تحميلها وحفظها محلياً
// أو مشاركتها باستخدام share_plus
```

---

## 🛡️ الأمان والخصوصية

### ✅ أفضل الممارسات

1. **لا ترفع مفاتيح API إلى Git**
   - أضف `.env` إلى `.gitignore`
   - استخدم متغيرات البيئة

2. **احفظ نسخة احتياطية**
   - احفظ مفاتيحك في مكان آمن
   - استخدم مدير كلمات السر

3. **راقب الاستخدام**
   - تابع استهلاك API عبر لوحة التحكم
   - ضع حدود للاستخدام

4. **غيّر المفاتيح عند التسريب**
   - إذا تم تسريب مفتاح، غيّره فورًا
   - راجع سجلات الاستخدام

### ⚠️ تحذيرات

- لا تشارك مفاتيحك مع أحد
- لا تضع المفاتيح في الكود المصدري
- استخدم HTTPS فقط للاتصال بالـ APIs

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من صحة مفاتيح API
2. راجع سجلات الأخطاء
3. تأكد من اتصالك بالإنترنت
4. اتصل بفريق الدعم

---

## 📝 التحديثات

- **v1.0.0** (2025-01-04)
  - ✅ إضافة دعم Nano Banana
  - ✅ إضافة دعم Google Veo
  - ✅ دعم نماذج متعددة للصور والفيديوهات
  - ✅ ملف تكوين مركزي للمفاتيح

---

تم التطوير بواسطة فريق Social Media Manager 🚀
