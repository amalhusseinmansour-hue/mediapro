# 🎯 اختبار ميزات التطبيق - Feature Testing Guide
# Feature Testing Guide for Ultimate Media Application

## 📋 نظرة عامة
هذا الدليل يوضح كيفية اختبار الميزات الأساسية للتطبيق بالطرق المختلفة:

### الميزات الأساسية الأربع:
1. ✅ **لوحة التحكم (Dashboard)** - عرض إحصائيات شاملة
2. ✅ **شاشة المحتوى (Content Screen)** - إدارة المنشورات
3. ✅ **شاشة التحليلات (Analytics Screen)** - بيانات الأداء
4. ✅ **شاشة إنشاء منشور (Create Post Screen)** - إنشاء محتوى جديد

---

## 🚀 طرق الاختبار

### الطريقة 1: اختبار عملي مباشر (Windows PowerShell)

```powershell
# تشغيل سكريبت الاختبار
cd c:\Users\HP\social_media_manager
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
.\Test-Features.ps1
```

**ماذا يحدث:**
- ✅ الاتصال بخادم API
- ✅ تسجيل دخول مسؤول
- ✅ اختبار جميع الـ 4 ميزات
- ✅ عرض النتائج بشكل ملون

**النتائج المتوقعة:**
```
✅ Dashboard: 15 users, 8 active subscriptions
✅ Content: 42 posts retrieved
✅ Analytics: 1,250 views, 89 likes, 12 comments
✅ Create Post: Post ID 1523 created successfully
```

---

### الطريقة 2: اختبار Unit Tests (PHP)

```bash
cd backend
./vendor/bin/phpunit tests/Feature/DashboardFeaturesTest.php
```

**الاختبارات:**

#### TEST 1: Dashboard Statistics
```php
→ يتحقق من وجود بيانات حقيقية
→ عدد المستخدمين الإجمالي
→ الاشتراكات النشطة
→ أرصدة المحافظ
→ تذاكر الدعم
→ إجمالي الإيرادات
```

#### TEST 2: Content Screen - Posts
```php
→ استرجاع قائمة المنشورات
→ تصفية حسب الحالة (published/draft)
→ الترقيم (Pagination)
→ معلومات المنشور الكاملة
```

#### TEST 3: Analytics - Real Data
```php
→ عدد المشاهدات الكلي
→ عدد الإعجابات الكلي
→ عدد التعليقات الكلي
→ عدد المشاركات الكلي
→ معدل التفاعل (Engagement Rate)
→ أفضل المنشورات (Top Posts)
→ إحصائيات يومية
```

#### TEST 4: Create Post
```php
→ إنشاء منشور جديد بنجاح
→ تعيين المنصات (Instagram, Facebook, Twitter)
→ جدولة المنشور المستقبلي
→ الحصول على اقتراحات محتوى AI
→ استخدام قوالب المحتوى
→ تحديث حالة المنشور
```

---

### الطريقة 3: اختبار API مباشر (Postman/cURL)

#### 3.1 الحصول على Token الإدارة
```bash
curl -X POST "https://mediaprosocial.io/api/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@mediapro.com",
    "password": "Admin@2025"
  }'
```

**الرد:**
```json
{
  "success": true,
  "message": "تم تسجيل الدخول بنجاح",
  "data": {
    "token": "eyJhbGc...token_here",
    "user": {
      "id": 1,
      "email": "admin@mediapro.com",
      "name": "Admin User"
    }
  }
}
```

#### 3.2 اختبار Dashboard
```bash
curl -X GET "https://mediaprosocial.io/api/admin/dashboard" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Accept: application/json"
```

**الرد المتوقع:**
```json
{
  "success": true,
  "data": {
    "users": {
      "total": 150,
      "active_subscribers": 89,
      "free_users": 61,
      "new_this_month": 15,
      "new_today": 2
    },
    "subscriptions": {
      "total": 89,
      "active": 87,
      "expired": 2
    },
    "wallets": {
      "total_balance": 12500.50,
      "total_wallets": 45,
      "active_wallets": 42
    },
    "requests": {
      "website_requests": 234,
      "sponsored_ads": 56,
      "bank_transfers": 12
    },
    "support": {
      "open_tickets": 3,
      "closed_tickets": 127,
      "total_tickets": 130
    },
    "revenue": {
      "total_revenue": 45000,
      "this_month": 5200,
      "this_week": 1250
    }
  }
}
```

#### 3.3 اختبار Content Screen
```bash
curl -X GET "https://mediaprosocial.io/api/posts?per_page=10&page=1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Accept: application/json"
```

#### 3.4 اختبار Analytics
```bash
curl -X GET "https://mediaprosocial.io/api/analytics" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Accept: application/json"
```

#### 3.5 اختبار Create Post
```bash
curl -X POST "https://mediaprosocial.io/api/posts" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "منتج جديد رائع",
    "content": "وصف المنتج",
    "platforms": ["instagram", "facebook", "twitter"],
    "status": "draft",
    "scheduled_at": "2025-01-15 10:00:00"
  }'
```

---

## ✅ معايير النجاح

### Dashboard ✅
- [ ] يتم عرض عدد المستخدمين الفعلي
- [ ] يتم عرض الاشتراكات النشطة
- [ ] يتم عرض أرصدة المحافظ
- [ ] يتم عرض تذاكر الدعم
- [ ] يتم عرض الإيرادات الفعلية

### Content Screen ✅
- [ ] يتم استرجاع قائمة المنشورات
- [ ] يتم عرض معلومات المنشور الكاملة
- [ ] يعمل الترقيم (Pagination)
- [ ] يمكن تصفية حسب الحالة
- [ ] يتم عرض أكثر من منشور واحد

### Analytics Screen ✅
- [ ] يتم عرض عدد المشاهدات الفعلي
- [ ] يتم عرض عدد الإعجابات الفعلي
- [ ] يتم عرض معدل التفاعل
- [ ] يتم عرض أفضل المنشورات
- [ ] يتم عرض الإحصائيات اليومية

### Create Post Screen ✅
- [ ] يتم إنشاء المنشور بنجاح
- [ ] يتم تعيين المنصات بشكل صحيح
- [ ] يتم جدولة المنشور إذا لزم الأمر
- [ ] يتم تحديث حالة المنشور
- [ ] يتم إرجاع معرف المنشور الجديد

---

## 🔧 استكشاف الأخطاء

### المشكلة: فشل التسجيل
**الحل:**
```
1. تحقق من بيانات المسؤول:
   - البريد: admin@mediapro.com
   - كلمة المرور: Admin@2025
   
2. تحقق من اتصال قاعدة البيانات
3. تأكد من أن جدول المستخدمين يحتوي على هذا الحساب
```

### المشكلة: لا توجد بيانات
**الحل:**
```
1. تحقق من أن قاعدة البيانات تحتوي على بيانات حقيقية
2. قم بإدراج بيانات تجريبية:
   - مستخدمين
   - منشورات
   - اشتراكات
3. تأكد من الأذونات الصحيحة للمسؤول
```

### المشكلة: الاتصال بـ API فشل
**الحل:**
```
1. تحقق من عنوان URL:
   - https://mediaprosocial.io/api
   
2. تحقق من شهادة SSL:
   - استخدم -k في cURL للتخطي
   
3. تحقق من أن الخادم يعمل:
   - curl -I https://mediaprosocial.io
```

---

## 📊 نماذج البيانات المتوقعة

### POST Response
```json
{
  "id": 123,
  "title": "عنوان المنشور",
  "content": "محتوى المنشور",
  "status": "published",
  "platforms": ["instagram", "facebook"],
  "views": 450,
  "likes": 32,
  "comments": 8,
  "shares": 2,
  "created_at": "2025-01-10T12:30:00",
  "updated_at": "2025-01-10T12:30:00"
}
```

### Analytics Response
```json
{
  "total_views": 15420,
  "total_likes": 1203,
  "total_comments": 284,
  "total_shares": 95,
  "engagement_rate": 8.5,
  "top_posts": [
    {
      "id": 1,
      "title": "أفضل منشور",
      "views": 2500,
      "engagement": 185
    }
  ],
  "daily_stats": [
    {
      "date": "2025-01-10",
      "views": 1240,
      "engagement": 95
    }
  ]
}
```

---

## 🎓 خطوات الاختبار التفصيلية

### خطوة 1: إعداد البيئة
```powershell
# تسجيل الدخول إلى السيرفر
ssh user@82.25.83.217

# التحقق من خدمة Laravel
cd /home/user/ultimate-media
php artisan status

# التحقق من قاعدة البيانات
mysql -h 82.25.83.217 -u u126213189_socialmedia_ma -p
SELECT COUNT(*) FROM users;
```

### خطوة 2: تشغيل الاختبارات
```powershell
# الطريقة الأولى: PowerShell
.\Test-Features.ps1

# الطريقة الثانية: PHP Unit Tests
cd backend
./vendor/bin/phpunit tests/Feature/DashboardFeaturesTest.php

# الطريقة الثالثة: cURL مباشر
curl -X GET "https://mediaprosocial.io/api/admin/dashboard" ...
```

### خطوة 3: التحقق من النتائج
- ✅ تم عرض بيانات حقيقية (وليس بيانات وهمية)
- ✅ لا توجد رسائل خطأ
- ✅ الأداء مقبول (< 2 ثانية)
- ✅ جميع المنصات تعمل بشكل صحيح

### خطوة 4: التوثيق
```
قم بحفظ النتائج:
- اسم الاختبار
- التاريخ والوقت
- النتائج (نجح/فشل)
- ملاحظات إضافية
```

---

## 📞 جهات الاتصال والدعم

**خادم التطبيق:**
- المضيف: 82.25.83.217
- قاعدة البيانات: u126213189_socialmedia_ma
- URL: https://mediaprosocial.io

**حسابات الاختبار:**
- البريد: admin@mediapro.com
- كلمة المرور: Admin@2025

**متطلبات التشغيل:**
- PHP 8.2+
- Laravel 11
- MySQL 5.7+
- Node.js 18+

---

## 📝 ملاحظات مهمة

1. **الأمان:** استخدم HTTPS فقط في الاتصالات
2. **التوافق:** تأكد من توافق إصدارات PHP و Laravel
3. **الأداء:** راقب استهلاك الموارد أثناء الاختبارات
4. **النسخ الاحتياطية:** قم بعمل نسخة احتياطية قبل الاختبارات

---

## 🎉 النتيجة النهائية

بعد إكمال جميع الاختبارات:
```
✅ جميع الميزات تعمل بكفاءة
✅ البيانات حقيقية وليست وهمية
✅ الأداء مقبول
✅ التطبيق جاهز للإنتاج
```

تم آخر تحديث: 2025-01-10 ✅
