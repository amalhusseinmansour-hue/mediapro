# ميديا برو - لوحة التحكم الويب
## Dashboard Web Application

نسخة ويب كاملة وحديثة لتطبيق ميديا برو لإدارة وسائل التواصل الاجتماعي، مشابهة لـ https://dashboard.thestring.net

---

## 📋 المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المميزات](#المميزات)
- [البنية التقنية](#البنية-التقنية)
- [التثبيت والاستخدام](#التثبيت-والاستخدام)
- [الملفات والمجلدات](#الملفات-والمجلدات)
- [الربط مع Backend API](#الربط-مع-backend-api)
- [التخصيص](#التخصيص)

---

## 🎯 نظرة عامة

لوحة تحكم ويب احترافية وكاملة لإدارة حسابات وسائل التواصل الاجتماعي، تتضمن:

- ✅ نظام تسجيل دخول وتسجيل حساب جديد
- ✅ لوحة تحكم رئيسية مع إحصائيات مباشرة
- ✅ رسوم بيانية تفاعلية للأداء
- ✅ إدارة المنشورات المجدولة
- ✅ تصميم عصري ومتجاوب (Responsive)
- ✅ تكامل كامل مع Laravel Backend API

---

## ✨ المميزات

### صفحات المصادقة (Authentication)

#### 1. صفحة تسجيل الدخول (`login.html`)
- تسجيل دخول بالبريد الإلكتروني وكلمة المرور
- تسجيل دخول سريع عبر Google و Facebook
- خيار "تذكرني"
- رابط استعادة كلمة المرور
- رسائل خطأ واضحة
- تصميم جذاب بجانبين (معلومات وفورم)

#### 2. صفحة التسجيل (`register.html`)
- إنشاء حساب جديد
- التسجيل السريع عبر Google و Facebook
- التحقق من صحة البيانات
- الموافقة على الشروط والأحكام
- كلمة مرور قوية (8 أحرف على الأقل)

#### 3. صفحة استعادة كلمة المرور (`forgot-password.html`)
- إرسال رابط إعادة التعيين عبر البريد
- واجهة بسيطة وسهلة

### لوحة التحكم الرئيسية (`index.html`)

#### Sidebar (الشريط الجانبي)
- قائمة تنقل كاملة
- أيقونات واضحة لكل قسم
- badges لعرض الأعداد والإشعارات
- بطاقة ترقية للخطة Pro

#### Header (الرأس العلوي)
- شريط بحث
- إشعارات مع عداد
- قائمة المستخدم المنسدلة
- زر إنشاء منشور جديد

#### المحتوى الرئيسي
1. **بطاقات الإحصائيات (Stats Cards)**
   - المنشورات المجدولة
   - إجمالي المتابعين
   - معدل التفاعل
   - الوصول الكلي
   - مؤشرات الزيادة/النقصان

2. **الرسوم البيانية (Charts)**
   - رسم بياني خطي للأداء (Performance Chart)
   - رسم بياني دائري للمنصات (Platform Distribution)
   - تفاعلية باستخدام Chart.js

3. **قائمة المنشورات القادمة**
   - عرض المنشورات المجدولة
   - أيقونات المنصات
   - توقيت النشر
   - حالة المنشور (pending, scheduled)
   - أزرار تعديل وحذف

4. **الإجراءات السريعة (Quick Actions)**
   - إنشاء منشور جديد
   - ربط حساب
   - توليد محتوى بالذكاء الاصطناعي
   - تقرير مفصل
   - نصيحة اليوم

---

## 🛠 البنية التقنية

### التقنيات المستخدمة

- **HTML5**: بنية الصفحات
- **CSS3**: التصميم والتنسيق
  - CSS Grid و Flexbox
  - CSS Variables للألوان والقيم
  - Animations و Transitions
  - Responsive Design
- **JavaScript (ES6+)**: البرمجة والتفاعل
  - Fetch API للاتصال بالـ Backend
  - LocalStorage لحفظ بيانات المستخدم
  - Event Listeners
- **Chart.js**: الرسوم البيانية التفاعلية
- **Font Awesome 6**: الأيقونات
- **IBM Plex Sans Arabic**: خط عربي احترافي من Google Fonts

### معمارية التصميم (Design System)

```css
/* نظام الألوان */
--primary-color: #0277d4      /* اللون الأساسي - أزرق */
--success-color: #00d4aa      /* نجاح - أخضر */
--warning-color: #ffa726      /* تحذير - برتقالي */
--error-color: #ff4757        /* خطأ - أحمر */

/* التباعدات (Spacing) */
--spacing-xs: 0.5rem
--spacing-sm: 1rem
--spacing-md: 1.5rem
--spacing-lg: 2rem
--spacing-xl: 3rem

/* الحدود المنحنية (Border Radius) */
--radius-sm: 0.5rem
--radius-md: 0.75rem
--radius-lg: 1rem

/* الظلال (Shadows) */
--shadow-sm: خفيف
--shadow-md: متوسط
--shadow-lg: كبير
```

---

## 📦 التثبيت والاستخدام

### 1. البنية المطلوبة

```
dashboard/
├── index.html                 # لوحة التحكم الرئيسية
├── login.html                 # صفحة تسجيل الدخول
├── register.html              # صفحة التسجيل
├── forgot-password.html       # صفحة استعادة كلمة المرور
├── assets/
│   ├── css/
│   │   ├── auth.css          # تصميم صفحات المصادقة
│   │   └── dashboard.css     # تصميم لوحة التحكم
│   └── js/
│       ├── auth.js           # منطق المصادقة
│       └── dashboard.js      # منطق لوحة التحكم
└── README.md
```

### 2. رفع الملفات على السيرفر

#### الطريقة الأولى: عبر FTP/SFTP

```bash
# رفع مجلد dashboard كاملاً إلى:
/home/u126213189/domains/mediaprosocial.io/public_html/dashboard/
```

#### الطريقة الثانية: عبر cPanel

1. افتح File Manager في cPanel
2. انتقل إلى `/public_html/`
3. أنشئ مجلد جديد باسم `dashboard`
4. ارفع جميع الملفات داخل المجلد

### 3. الوصول للتطبيق

بعد الرفع، يمكن الوصول عبر:

```
https://mediaprosocial.io/dashboard/login.html
```

---

## 📁 الملفات والمجلدات

### ملفات HTML

#### `login.html`
صفحة تسجيل الدخول:
- فورم تسجيل الدخول
- تسجيل دخول اجتماعي (Google, Facebook)
- استعادة كلمة المرور
- رابط للتسجيل

#### `register.html`
صفحة إنشاء حساب:
- فورم التسجيل
- التسجيل عبر Google/Facebook
- التحقق من البيانات
- الموافقة على الشروط

#### `forgot-password.html`
صفحة استعادة كلمة المرور:
- إدخال البريد الإلكتروني
- إرسال رابط إعادة التعيين

#### `index.html`
لوحة التحكم الرئيسية:
- Sidebar مع قائمة التنقل
- Header مع البحث والإشعارات
- بطاقات الإحصائيات
- الرسوم البيانية
- قائمة المنشورات
- الإجراءات السريعة

### ملفات CSS

#### `auth.css`
تصميم صفحات المصادقة:
- Layout بجانبين
- Forms styling
- Buttons
- Animations
- Responsive design

#### `dashboard.css`
تصميم لوحة التحكم:
- Sidebar layout
- Header styling
- Cards و Grids
- Charts containers
- Responsive layout

### ملفات JavaScript

#### `auth.js`
منطق المصادقة:
- `login()`: تسجيل الدخول
- `register()`: التسجيل
- `forgotPassword()`: استعادة كلمة المرور
- `logout()`: تسجيل الخروج
- `checkAuth()`: التحقق من المصادقة
- `apiCall()`: استدعاء API مع Token

#### `dashboard.js`
منطق لوحة التحكم:
- `loadUserData()`: تحميل بيانات المستخدم
- `initCharts()`: إنشاء الرسوم البيانية
- `loadDashboardStats()`: تحميل الإحصائيات
- Event listeners للتفاعل

---

## 🔗 الربط مع Backend API

### إعدادات API

في ملف `auth.js`:

```javascript
const API_URL = 'https://mediaprosocial.io/api';
```

### نقاط النهاية المطلوبة (Endpoints)

يجب أن يوفر الـ Laravel Backend هذه النقاط:

#### 1. المصادقة

```javascript
// تسجيل الدخول
POST /api/login
Body: { email, password, remember }
Response: { success: true, token: "...", user: {...} }

// التسجيل
POST /api/register
Body: { name, email, password, password_confirmation }
Response: { success: true, token: "...", user: {...} }

// استعادة كلمة المرور
POST /api/forgot-password
Body: { email }
Response: { success: true, message: "..." }
```

#### 2. لوحة التحكم

```javascript
// إحصائيات لوحة التحكم
GET /api/dashboard/stats
Headers: { Authorization: "Bearer {token}" }
Response: {
  success: true,
  stats: {
    scheduled_posts: 24,
    total_followers: 45200,
    engagement_rate: 7.8,
    total_reach: 125000
  }
}

// المنشورات المجدولة
GET /api/posts/scheduled
Headers: { Authorization: "Bearer {token}" }
Response: {
  success: true,
  posts: [...]
}
```

### التعامل مع Token

```javascript
// حفظ Token بعد تسجيل الدخول
localStorage.setItem('auth_token', data.token);

// إرسال Token مع كل طلب
headers: {
  'Authorization': `Bearer ${token}`
}

// حذف Token عند تسجيل الخروج
localStorage.removeItem('auth_token');
```

---

## 🎨 التخصيص

### تغيير الألوان

في ملف `auth.css` أو `dashboard.css`:

```css
:root {
    --primary-color: #0277d4;  /* غيّر هذا للون الأساسي */
    --success-color: #00d4aa;  /* لون النجاح */
    --error-color: #ff4757;    /* لون الخطأ */
}
```

### تغيير الخط

في ملف HTML، غيّر رابط Google Fonts:

```html
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700&display=swap" rel="stylesheet">
```

ثم في CSS:

```css
body {
    font-family: 'Cairo', sans-serif;
}
```

### إضافة صفحات جديدة

1. أنشئ ملف HTML جديد (مثلاً `analytics.html`)
2. استخدم نفس البنية من `index.html`
3. أضف رابط في Sidebar:

```html
<li class="nav-item">
    <a href="analytics.html" class="nav-link">
        <i class="fas fa-chart-pie"></i>
        <span>التحليلات</span>
    </a>
</li>
```

---

## 📱 Responsive Design

التطبيق متجاوب بالكامل مع جميع الشاشات:

- **Desktop** (> 1024px): عرض كامل مع Sidebar
- **Tablet** (768px - 1024px): Sidebar قابل للإخفاء
- **Mobile** (< 768px): Sidebar منسدل، تصميم مبسط

---

## 🔒 الأمان

### Best Practices المطبقة:

1. **Token-based Authentication**: استخدام JWT Tokens
2. **Input Validation**: التحقق من المدخلات قبل الإرسال
3. **HTTPS**: استخدام SSL للاتصال الآمن
4. **Password Requirements**: كلمة مرور قوية (8+ أحرف)
5. **Session Management**: حفظ Token في LocalStorage
6. **Logout Functionality**: حذف Token عند الخروج

---

## 📞 الدعم

للمساعدة أو الاستفسارات:
- البريد الإلكتروني: support@mediaprosocial.io
- الموقع: https://mediaprosocial.io

---

## 📝 الترخيص

© 2024 ميديا برو. جميع الحقوق محفوظة.

---

## 🚀 الخطوات التالية

بعد نشر Dashboard:

1. ✅ اختبار تسجيل الدخول والتسجيل
2. ✅ التأكد من عمل الاتصال مع Backend API
3. ⏳ إضافة صفحات إضافية (المنشورات، التحليلات، الحسابات)
4. ⏳ إضافة ميزات متقدمة (Drag & Drop، Rich Text Editor)
5. ⏳ تحسين الأداء (Lazy Loading، Caching)

---

**تم إنشاء هذا التطبيق بواسطة Claude Code 🤖**
