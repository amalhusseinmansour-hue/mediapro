# 🔗 دليل إدارة حسابات السوشال ميديا المربوطة للمستخدمين

## 📌 نظرة عامة

تم إضافة صفحة جديدة في لوحة التحكم لإدارة حسابات السوشال ميديا المربوطة من قبل المستخدمين.

---

## 📍 الوصول إلى الصفحة

```
https://mediaprosocial.io/admin/connected-accounts
```

أو من القائمة الجانبية:
```
المستخدمين → حسابات المستخدمين المربوطة
```

---

## ✨ الميزات الرئيسية

### 1. عرض شامل للحسابات المربوطة
- عرض جميع حسابات السوشال ميديا المربوطة من قبل المستخدمين
- معلومات تفصيلية عن كل حساب
- حالة الحساب (نشط / غير نشط)
- حالة الـ Token (صالح / منتهي)

### 2. تبويبات ذكية
تم تنظيم الحسابات في تبويبات:
- **الكل** - جميع الحسابات
- **نشط** - الحسابات النشطة فقط
- **غير نشط** - الحسابات المعطلة
- **Facebook** - حسابات فيسبوك فقط
- **Instagram** - حسابات إنستغرام فقط
- **Twitter** - حسابات تويتر فقط
- **Token منتهي** - حسابات تحتاج تجديد Token

### 3. الفلاتر المتقدمة
يمكن تصفية الحسابات حسب:
- المنصة (متعدد الاختيار)
- المستخدم (متعدد الاختيار)
- الحالة (نشط / غير نشط)
- Token منتهي
- مستخدم مؤخراً (خلال 7 أيام)

---

## 📊 الأعمدة المعروضة

| العمود | الوصف |
|--------|-------|
| **المستخدم** | اسم المستخدم صاحب الحساب |
| **المنصة** | المنصة المربوطة (Facebook، Instagram، إلخ) |
| **اسم المستخدم** | Username في المنصة |
| **الاسم المعروض** | Display Name |
| **الصورة** | صورة الملف الشخصي |
| **نشط** | حالة الحساب |
| **انتهاء Token** | تاريخ انتهاء صلاحية Token |
| **تاريخ الربط** | متى تم ربط الحساب |
| **آخر استخدام** | آخر مرة تم استخدام الحساب |

---

## 🎯 الإجراءات المتاحة

### على مستوى الصف الواحد:

1. **عرض** - عرض تفاصيل الحساب كاملة
2. **تعديل** - تعديل معلومات الحساب
3. **تفعيل/تعطيل** - تبديل حالة الحساب
4. **تحديث Token** - تحديث Access Token و Refresh Token
5. **حذف** - حذف الحساب المربوط

### الإجراءات الجماعية (Bulk Actions):

- **تفعيل المحدد** - تفعيل عدة حسابات دفعة واحدة
- **تعطيل المحدد** - تعطيل عدة حسابات دفعة واحدة
- **حذف المحدد** - حذف عدة حسابات دفعة واحدة

---

## 📝 إضافة حساب جديد

### الخطوات:

1. اضغط على زر **"إضافة حساب"**
2. املأ الحقول المطلوبة:

#### معلومات الحساب:
- **المستخدم** - اختر المستخدم
- **المنصة** - اختر المنصة (Facebook، Instagram، إلخ)
- **معرف المستخدم في المنصة** - Platform User ID
- **اسم المستخدم** - Username
- **الاسم المعروض** - Display Name
- **البريد الإلكتروني** - Email
- **صورة الملف الشخصي** - رفع صورة

#### معلومات الاتصال:
- **Access Token** - الـ Token الرئيسي
- **Refresh Token** - لتجديد الـ Access Token
- **تاريخ انتهاء الـ Token** - متى ينتهي
- **الصلاحيات (Scopes)** - قائمة بالصلاحيات
- **الحساب نشط** - تفعيل/تعطيل
- **تاريخ الربط** - متى تم الربط

#### بيانات إضافية:
- **Metadata** - أي بيانات إضافية (Key-Value)

3. اضغط **"حفظ"**

---

## 🔍 عرض تفاصيل الحساب

عند الضغط على **"عرض"**، يتم عرض:

### 1. معلومات المستخدم
- اسم المستخدم
- البريد الإلكتروني

### 2. معلومات الحساب المربوط
- المنصة (مع badge ملون)
- معرف المنصة
- اسم المستخدم
- الاسم المعروض
- البريد الإلكتروني
- الصورة الشخصية
- الحالة (نشط/غير نشط)

### 3. معلومات الـ Token
- Access Token (مع إمكانية النسخ)
- تاريخ انتهاء Token (مع مؤشر الحالة)
- الصلاحيات (Scopes)

### 4. التواريخ
- تاريخ الربط
- آخر استخدام (مع "منذ")
- تاريخ الإنشاء
- آخر تحديث

### 5. البيانات الإضافية
- عرض جميع الـ Metadata

---

## 🔄 تحديث Token

عند انتهاء صلاحية Token:

1. اضغط على **"تحديث Token"** من قائمة الإجراءات
2. املأ النموذج:
   - **Access Token الجديد**
   - **Refresh Token الجديد** (اختياري)
   - **تاريخ الانتهاء الجديد**
3. اضغط **"حفظ"**

---

## 🎨 الألوان والأيقونات

### ألوان المنصات:

- 📘 **Facebook** - أزرق (#1877F2)
- 📸 **Instagram** - وردي (#E4405F)
- 🐦 **Twitter** - أزرق فاتح (#1DA1F2)
- 💼 **LinkedIn** - أزرق غامق (#0A66C2)
- 🎵 **TikTok** - أسود (#000000)
- 📺 **YouTube** - أحمر (#FF0000)
- 📌 **Pinterest** - أحمر غامق (#E60023)
- ✈️ **Telegram** - أزرق (#0088CC)

### مؤشرات الحالة:

- ✅ **نشط** - أخضر
- ❌ **غير نشط** - أحمر
- ⚠️ **Token منتهي** - برتقالي
- ✔️ **Token صالح** - أخضر

---

## 🔔 التحديث التلقائي

الصفحة تُحدث تلقائياً كل **30 ثانية** لعرض أحدث البيانات.

---

## 📊 Badge العدد

يظهر في القائمة الجانبية عدد الحسابات النشطة بجانب اسم الصفحة.

---

## 🗂️ هيكل قاعدة البيانات

### جدول `social_accounts`

```sql
CREATE TABLE social_accounts (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNSIGNED NOT NULL,
    platform VARCHAR(50) NOT NULL,
    platform_user_id VARCHAR(255),
    username VARCHAR(255),
    display_name VARCHAR(255),
    profile_picture TEXT,
    email VARCHAR(255),
    access_token TEXT,
    refresh_token TEXT,
    token_expires_at TIMESTAMP NULL,
    scopes JSON,
    is_active BOOLEAN DEFAULT TRUE,
    connected_at TIMESTAMP,
    last_used_at TIMESTAMP NULL,
    metadata JSON,
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    deleted_at TIMESTAMP NULL,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

### الحقول المخفية (Hidden):
- `access_token`
- `refresh_token`

هذه الحقول لا تظهر في API للحفاظ على الأمان.

---

## 🔐 الأمان

### الصلاحيات:
- فقط المسؤولون يمكنهم الوصول لهذه الصفحة
- الـ Tokens محمية ومخفية
- Soft Deletes مفعّل للحذف الآمن

### Best Practices:
1. لا تشارك الـ Tokens مع أحد
2. قم بتجديد الـ Tokens دورياً
3. تحقق من الصلاحيات (Scopes) قبل الحفظ
4. عطّل الحسابات غير المستخدمة

---

## 📈 حالات الاستخدام

### 1. مراقبة الحسابات المربوطة
```
المسؤول يريد معرفة:
- كم عدد المستخدمين الذين ربطوا حساباتهم؟
- أي منصة هي الأكثر استخداماً؟
- هل هناك Tokens منتهية تحتاج تجديد؟
```

### 2. دعم المستخدمين
```
مستخدم يواجه مشكلة في النشر:
- المسؤول يفتح حساب المستخدم
- يتحقق من حالة Token
- يجدد Token إذا لزم الأمر
- يفعّل الحساب إذا كان معطلاً
```

### 3. صيانة دورية
```
كل أسبوع:
- فلتر: "Token منتهي"
- تحديث جماعي للـ Tokens
- أو تعطيل الحسابات منتهية الصلاحية
```

---

## 🛠️ المشاكل الشائعة

### المشكلة: Token منتهي

**الحل:**
1. اذهب للحساب
2. اضغط "تحديث Token"
3. احصل على Token جديد من المنصة
4. احفظ

---

### المشكلة: المستخدم لا يستطيع النشر

**الحل:**
1. تحقق من حالة الحساب (is_active)
2. تحقق من Token
3. تحقق من الصلاحيات (Scopes)
4. تحقق من آخر استخدام

---

### المشكلة: حساب معطّل تلقائياً

**الحل:**
```
قد يكون السبب:
- Token منتهي
- المنصة ألغت الاتصال
- المستخدم ألغى الأذونات

الخطوات:
1. تواصل مع المستخدم
2. اطلب منه إعادة الربط
3. أو جدد Token يدوياً
```

---

## 📱 التكامل مع التطبيق

### API Endpoints:

```php
// جلب حسابات المستخدم المربوطة
GET /api/user/connected-accounts

// ربط حساب جديد
POST /api/social-accounts/connect

// إلغاء ربط حساب
DELETE /api/social-accounts/{id}/disconnect

// تجديد Token
POST /api/social-accounts/{id}/refresh-token
```

---

## 📊 الإحصائيات

يمكنك معرفة:
- **إجمالي الحسابات المربوطة**
- **عدد الحسابات النشطة**
- **عدد الحسابات لكل منصة**
- **عدد الـ Tokens المنتهية**

كل ذلك من التبويبات في أعلى الصفحة.

---

## 🎯 نصائح الاستخدام

### ✅ افعل:
1. راقب الـ Tokens المنتهية بانتظام
2. عطّل الحسابات غير المستخدمة منذ مدة طويلة
3. استخدم الفلاتر لإيجاد الحسابات المحددة
4. استخدم Bulk Actions للعمليات الجماعية

### ❌ لا تفعل:
1. لا تحذف الحسابات بدون سبب
2. لا تشارك الـ Tokens
3. لا تترك Tokens منتهية بدون معالجة
4. لا تعطّل حسابات نشطة بدون تنبيه المستخدم

---

## 🔄 التحديثات القادمة

### ميزات مقترحة:
- [ ] إشعارات تلقائية عند انتهاء Token
- [ ] تجديد تلقائي للـ Tokens (Auto Refresh)
- [ ] رسم بياني لاستخدام الحسابات
- [ ] تصدير البيانات (Excel/CSV)
- [ ] سجل نشاط الحساب (Activity Log)

---

## 📞 الدعم

في حالة وجود مشاكل:
1. تحقق من Logs: `/storage/logs/laravel.log`
2. تحقق من قاعدة البيانات مباشرة
3. استخدم Tinker للتحقق من البيانات

---

## 📝 الخلاصة

تم إضافة صفحة شاملة لإدارة حسابات السوشال ميديا المربوطة:

✅ **عرض تفصيلي** - جميع المعلومات في مكان واحد
✅ **تبويبات ذكية** - تنظيم حسب المنصة والحالة
✅ **فلاتر متقدمة** - بحث وتصفية سريعة
✅ **إجراءات جماعية** - تفعيل/تعطيل/حذف جماعي
✅ **تحديث Tokens** - تجديد الـ Tokens المنتهية
✅ **واجهة احترافية** - ألوان وأيقونات مميزة
✅ **تحديث تلقائي** - كل 30 ثانية

---

**الرابط:** `https://mediaprosocial.io/admin/connected-accounts`

**آخر تحديث:** نوفمبر 2024
**الحالة:** ✅ جاهز للاستخدام
