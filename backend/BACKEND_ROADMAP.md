# 🗺️ خارطة طريق تطوير الواجهة الخلفية (Backend)

هذا المستند يوضح المهام المتبقية لإكمال الواجهة الخلفية لتطبيق "ميديا برو" وجعلها جاهزة للإطلاق الكامل.

---

## 🎯 المرحلة الأولى: الأمان وتجهيز البيئة (الأسبوع 1)

-   [x] **إزالة بيانات الاعتماد الحساسة من الكود المصدري.** (تم)
    -   تم تنظيف ملف `run_fix_on_server.bat`.
    -   تم حذف ملفات `LOGIN_*.txt`.
-   [ ] **تطبيق استخدام متغيرات البيئة (`.env`) لجميع المفاتيح.**
    -   يجب التأكد من أن جميع مفاتيح API (مثل OpenAI, Google, Facebook) وكلمات المرور يتم تحميلها من ملف `.env` على الخادم فقط.
-   [ ] **تكوين مفاتيح SSH للوصول الآمن للخادم.**
    -   يجب تعطيل تسجيل الدخول بكلمة المرور والاعتماد على SSH Keys.

---

## 🚀 المرحلة الثانية: إكمال الميزات الأساسية (الأسبوع 1-2)

-   [ ] **تكامل Firebase الكامل:**
    -   [ ] تفعيل مصادقة المستخدمين عبر رقم الهاتف (Firebase Phone Authentication).
    -   [ ] إعداد Cloud Storage لتخزين الصور والملفات التي يرفعها المستخدمون.
    -   [ ] إنشاء Rules لـ Cloud Storage لتأمين الملفات.
-   [ ] **إنشاء Endpoint للتحكم في خدمة تعديل الصور:**
    -   بدلاً من أن يتصل التطبيق مباشرة بـ N8N، يجب إنشاء Endpoint في الواجهة الخلفية (Laravel) ليكون وسيطًا.
    -   `POST /api/v1/images/edit` (جاري العمل عليه الآن)

---

## 🔗 المرحلة الثالثة: الربط مع واجهات برمجة التطبيقات الخارجية (الأسبوع 2-3)

-   [ ] **إنشاء وحدات (Modules) للربط مع منصات التواصل الاجتماعي:**
    -   [ ] وحدة Facebook Graph API.
    -   [ ] وحدة Instagram API.
    -   [ ] وحدة Twitter/X API.
    -   [ ] وحدة LinkedIn API.
-   [ ] **إنشاء Endpoints لنشر المحتوى وجلب التحليلات:**
    -   `POST /api/v1/posts/schedule`
    -   `GET /api/v1/analytics/{platform}`

---

## 🧠 المرحلة الرابعة: تفعيل الذكاء الاصطناعي وأنظمة الدفع (الأسبوع 3-4)

-   [ ] **إنشاء Endpoints لتوليد المحتوى:**
    -   `POST /api/v1/ai/generate-text` (للربط مع OpenAI/Gemini).
-   [ ] **تكامل نظام الدفع (Payment Gateway):**
    -   إنشاء Endpoints لإدارة الاشتراكات والتحقق من عمليات الدفع.