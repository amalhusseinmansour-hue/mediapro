# دليل سريع: كيفية إضافة مفاتيح Twitter API

## الطريقة الأسهل (للتطوير والاختبار)

### الخطوة 1: الحصول على مفاتيح Twitter

1. اذهب إلى: **https://developer.twitter.com/en/portal/dashboard**
2. سجل الدخول بحسابك في Twitter/X
3. اضغط على **"+ Create Project"**
4. أدخل اسم المشروع: **"Social Media Manager"**
5. اختر الاستخدام: **"Making a bot"** أو **"Exploring the API"**
6. اضغط على **"+ Create App"** داخل المشروع
7. أدخل اسم التطبيق: **"MediaPro"**
8. **احفظ المفاتيح فوراً!**:
   - API Key (Consumer Key)
   - API Secret Key (Consumer Secret)

### الخطوة 2: تفعيل OAuth 1.0a

1. في صفحة التطبيق، اذهب إلى **"Settings"**
2. اضغط على **"Set up"** في User authentication settings
3. اختر نوع OAuth: **"OAuth 1.0a"**
4. صلاحيات التطبيق: **"Read and Write"**
5. أضف Callback URL:
   ```
   social-media-manager://callback
   ```
6. أضف Website URL (أي رابط صحيح):
   ```
   https://mediaprosocial.io
   ```
7. احفظ التغييرات

### الخطوة 3: إضافة المفاتيح للتطبيق

افتح الملف:
```
lib/core/config/api_config.dart
```

ابحث عن السطور (149-159):

```dart
/// Twitter API Keys
static const String twitterApiKey = String.fromEnvironment(
  'TWITTER_API_KEY',
  defaultValue: '',
);

static const String twitterApiSecret = String.fromEnvironment(
  'TWITTER_API_SECRET',
  defaultValue: '',
);
```

**استبدلها بـ:**

```dart
/// Twitter API Keys
static const String twitterApiKey = String.fromEnvironment(
  'TWITTER_API_KEY',
  defaultValue: 'ضع_API_Key_هنا', // ألصق مفتاحك هنا
);

static const String twitterApiSecret = String.fromEnvironment(
  'TWITTER_API_SECRET',
  defaultValue: 'ضع_API_Secret_هنا', // ألصق المفتاح السري هنا
);
```

### الخطوة 4: تشغيل التطبيق

```bash
# نظف المشروع
flutter clean

# احصل على الحزم
flutter pub get

# شغّل التطبيق
flutter run -d R9KY902X3HW
```

الآن يمكنك الضغط على **"ربط Twitter"** في التطبيق وسيعمل بشكل صحيح!

---

## مثال عملي

إذا كانت مفاتيحك:
- **API Key**: `abc123xyz456def789`
- **API Secret**: `secret123secret456secret789`

ضعها هكذا في `api_config.dart`:

```dart
static const String twitterApiKey = String.fromEnvironment(
  'TWITTER_API_KEY',
  defaultValue: 'abc123xyz456def789',
);

static const String twitterApiSecret = String.fromEnvironment(
  'TWITTER_API_SECRET',
  defaultValue: 'secret123secret456secret789',
);
```

---

## ⚠️ تحذير أمني

**للتطوير فقط!** لا تضع المفاتيح الحقيقية في الكود إذا كنت ستشاركه أو ترفعه على GitHub!

**للإنتاج:**
استخدم Environment Variables عند تشغيل التطبيق:

```bash
flutter run -d R9KY902X3HW \
  --dart-define=TWITTER_API_KEY=your_real_key \
  --dart-define=TWITTER_API_SECRET=your_real_secret
```

---

## استكشاف الأخطاء

### ❌ "يجب تكوين Twitter API Keys أولاً"
**الحل:** تأكد من وضع المفاتيح في `defaultValue` في api_config.dart

### ❌ "Callback URL mismatch"
**الحل:** أضف `social-media-manager://callback` في إعدادات التطبيق على Twitter

### ❌ "401 Unauthorized"
**الحل:** تحقق من صحة المفاتيح، قد تحتاج لتجديدها

### ❌ "403 Forbidden"
**الحل:** فعّل OAuth 1.0a في إعدادات التطبيق

---

## 💡 نصائح

1. احفظ المفاتيح في مكان آمن فور الحصول عليها
2. لا تشارك المفاتيح مع أحد
3. يمكنك تجديد المفاتيح في أي وقت من Developer Portal
4. في وضع Development، يمكن ربط 15 حساب فقط
5. للحصول على حدود أعلى، قدّم طلب Elevated Access

---

للمزيد من التفاصيل، راجع: **TWITTER_SETUP.md**
