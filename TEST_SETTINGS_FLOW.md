# 🧪 اختبار تدفق الإعدادات (Settings Flow Test)

## اختبار سريع لمعرفة كيف تعمل الإعدادات

---

## 🎯 الهدف
التأكد من أن التعديلات في لوحة التحكم تظهر في تطبيق الموبايل

---

## 📋 خطوات الاختبار

### 1️⃣ اختبار تغيير اسم التطبيق

#### الخطوة 1: في لوحة التحكم
```
1. افتح: https://mediaprosocial.io/admin/manage-app-settings
2. اذهب لـ Tab "معلومات التطبيق"
3. غير "اسم التطبيق (عربي)" من "ميديا برو" إلى "تطبيق تجريبي"
4. اضغط "حفظ جميع الإعدادات"
5. انتظر إشعار "✅ تم الحفظ بنجاح"
```

#### الخطوة 2: اختبر API مباشرة
```bash
curl https://mediaprosocial.io/api/settings/app-config
```

**النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": {
    "app": {
      "name": "تطبيق تجريبي",  // ✅ الاسم الجديد
      ...
    }
  }
}
```

#### الخطوة 3: في تطبيق الموبايل
```
1. أغلق التطبيق تماماً (Force Close)
2. افتحه مرة أخرى
3. تحقق من اسم التطبيق في الواجهة
```

**النتيجة المتوقعة:**
- ✅ يظهر "تطبيق تجريبي" بدلاً من "ميديا برو"

#### الخطوة 4: أرجع الاسم الأصلي
```
1. ارجع لوحة التحكم
2. غير الاسم إلى "ميديا برو"
3. احفظ
```

---

### 2️⃣ اختبار وضع الصيانة

#### الخطوة 1: تفعيل الصيانة
```
1. افتح: https://mediaprosocial.io/admin/manage-app-settings
2. اذهب لـ Tab "التحديثات والصيانة"
3. فعّل "تفعيل وضع الصيانة"
4. اكتب رسالة: "اختبار وضع الصيانة - سنعود خلال دقيقة"
5. احفظ ✅
```

#### الخطوة 2: اختبر API
```bash
curl https://mediaprosocial.io/api/settings/app-config
```

**النتيجة المتوقعة:**
```json
{
  "app": {
    "maintenance_mode": true,  // ✅ مفعّل
    "maintenance_message": "اختبار وضع الصيانة - سنعود خلال دقيقة"
  }
}
```

#### الخطوة 3: في تطبيق الموبايل
```
1. أغلق التطبيق
2. افتحه مرة أخرى
```

**النتيجة المتوقعة:**
- ✅ يظهر شاشة صيانة
- ✅ تظهر الرسالة: "اختبار وضع الصيانة - سنعود خلال دقيقة"
- ❌ لا يمكن استخدام التطبيق

#### الخطوة 4: إلغاء الصيانة
```
1. ارجع لوحة التحكم
2. عطّل "تفعيل وضع الصيانة"
3. احفظ ✅
```

#### الخطوة 5: تحقق من العودة
```
1. أغلق التطبيق
2. افتحه مرة أخرى
```

**النتيجة المتوقعة:**
- ✅ التطبيق يعمل بشكل طبيعي
- ✅ لا توجد شاشة صيانة

---

### 3️⃣ اختبار تعطيل ميزة الدفع

#### الخطوة 1: تعطيل الدفع
```
1. افتح: https://mediaprosocial.io/admin/manage-app-settings
2. اذهب لـ Tab "الميزات"
3. عطّل "تفعيل نظام الدفع"
4. احفظ ✅
```

#### الخطوة 2: اختبر API
```bash
curl https://mediaprosocial.io/api/settings/app-config
```

**النتيجة المتوقعة:**
```json
{
  "features": {
    "payment_enabled": false  // ✅ معطّل
  }
}
```

#### الخطوة 3: في تطبيق الموبايل
```
1. أغلق التطبيق
2. افتحه مرة أخرى
3. اذهب لصفحة الباقات أو الدفع
```

**النتيجة المتوقعة:**
- ✅ خيارات الدفع مخفية
- ✅ صفحة الباقات لا تظهر
- ✅ زر "اشتراك" مخفي

#### الخطوة 4: إعادة تفعيل الدفع
```
1. ارجع لوحة التحكم
2. فعّل "تفعيل نظام الدفع"
3. احفظ ✅
```

---

### 4️⃣ اختبار تغيير العملة

#### الخطوة 1: تغيير العملة
```
1. افتح: https://mediaprosocial.io/admin/manage-app-settings
2. اذهب لـ Tab "إعدادات عامة"
3. غير العملة من "AED" إلى "USD"
4. احفظ ✅
```

#### الخطوة 2: اختبر API
```bash
curl https://mediaprosocial.io/api/settings/app-config
```

**النتيجة المتوقعة:**
```json
{
  "localization": {
    "currency": "USD"  // ✅ الدولار
  }
}
```

#### الخطوة 3: في تطبيق الموبايل
```
1. أغلق التطبيق
2. افتحه مرة أخرى
3. اذهب لأي صفحة تعرض أسعار
```

**النتيجة المتوقعة:**
- ✅ الأسعار تظهر بـ USD بدلاً من AED

#### الخطوة 4: أرجع العملة
```
1. ارجع لوحة التحكم
2. غير العملة إلى "AED"
3. احفظ ✅
```

---

### 5️⃣ اختبار تغيير الألوان

#### الخطوة 1: تغيير اللون الأساسي
```
1. افتح: https://mediaprosocial.io/admin/manage-app-settings
2. اذهب لـ Tab "معلومات التطبيق"
3. في قسم "الألوان والتصميم"
4. غير "اللون الأساسي" إلى #FF0000 (أحمر)
5. احفظ ✅
```

#### الخطوة 2: اختبر API
```bash
curl https://mediaprosocial.io/api/settings/app-config
```

#### الخطوة 3: في تطبيق الموبايل
```
1. أغلق التطبيق تماماً
2. امسح بيانات التطبيق (اختياري)
3. افتح التطبيق
```

**النتيجة المتوقعة:**
- 🎨 الألوان قد تتغير (يعتمد على implementation)
- 💡 قد تحتاج إعادة تثبيت لرؤية التغيير الكامل

#### الخطوة 4: أرجع اللون الأصلي
```
1. ارجع لوحة التحكم
2. غير اللون الأساسي إلى #6366F1 (الأزرق البنفسجي)
3. احفظ ✅
```

---

## 📊 جدول النتائج

| الاختبار | النتيجة | ملاحظات |
|---------|---------|---------|
| ✅ تغيير اسم التطبيق | ☐ نجح / ☐ فشل | |
| ✅ وضع الصيانة (تفعيل) | ☐ نجح / ☐ فشل | |
| ✅ وضع الصيانة (إلغاء) | ☐ نجح / ☐ فشل | |
| ✅ تعطيل الدفع | ☐ نجح / ☐ فشل | |
| ✅ تفعيل الدفع | ☐ نجح / ☐ فشل | |
| ✅ تغيير العملة | ☐ نجح / ☐ فشل | |
| ✅ تغيير الألوان | ☐ نجح / ☐ فشل | |

---

## 🔍 نقاط التحقق

### ✅ قبل كل اختبار:
- [ ] لوحة التحكم تعمل
- [ ] Backend API متصل
- [ ] التطبيق مثبت ويعمل

### ✅ بعد كل تعديل:
- [ ] ظهر إشعار "تم الحفظ بنجاح"
- [ ] API يعيد البيانات الجديدة
- [ ] التطبيق تم إغلاقه وإعادة فتحه

### ✅ النتيجة النهائية:
- [ ] جميع الاختبارات نجحت
- [ ] الإعدادات تُطبق فوراً
- [ ] لا توجد أخطاء

---

## 🐛 في حالة فشل الاختبار

### المشكلة: التعديلات لا تظهر في API

**الحل:**
```bash
# 1. امسح الـ cache في Backend
cd /home/u126213189/domains/mediaprosocial.io/public_html
php artisan cache:clear

# 2. اختبر API مباشرة
curl https://mediaprosocial.io/api/settings/app-config
```

---

### المشكلة: التعديلات لا تظهر في التطبيق

**الحل:**
```
1. تأكد من إغلاق التطبيق تماماً (Force Close)
2. امسح بيانات التطبيق (Clear Data)
3. افتح التطبيق مرة أخرى
4. تحقق من logs في التطبيق
```

---

### المشكلة: خطأ عند الحفظ

**الحل:**
```
1. تحقق من صحة البيانات المدخلة
2. تأكد من الاتصال بالإنترنت
3. حدّث الصفحة وجرب مرة أخرى
4. تحقق من logs في Backend
```

---

## 📱 اختبار سريع (Quick Test)

### 1 دقيقة فقط:

```
1. افتح لوحة التحكم
   https://mediaprosocial.io/admin/manage-app-settings

2. غير "اسم التطبيق (عربي)" إلى "TEST"

3. احفظ ✅

4. اختبر API:
   curl https://mediaprosocial.io/api/settings/app-config

5. أغلق التطبيق وافتحه

6. هل ظهر "TEST"؟
   ✅ نعم → النظام يعمل!
   ❌ لا → هناك مشكلة

7. أرجع الاسم إلى "ميديا برو"
```

---

## 🎯 الخلاصة

بعد إجراء هذه الاختبارات، ستتأكد من:

✅ لوحة التحكم تحفظ الإعدادات بشكل صحيح
✅ API يعيد البيانات المحدثة
✅ تطبيق الموبايل يجلب الإعدادات الجديدة
✅ التعديلات تُطبق فوراً عند إعادة فتح التطبيق

---

**🔗 روابط مفيدة:**

- لوحة التحكم: https://mediaprosocial.io/admin/manage-app-settings
- API Endpoint: https://mediaprosocial.io/api/settings/app-config
- التوثيق الكامل: `HOW_SETTINGS_WORK.md`
